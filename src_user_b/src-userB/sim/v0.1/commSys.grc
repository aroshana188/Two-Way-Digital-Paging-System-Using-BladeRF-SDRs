options:
  parameters:
    author: Geeth Sathsara
    catch_exceptions: 'True'
    category: '[GRC Hier Blocks]'
    cmake_opt: ''
    comment: ''
    copyright: ''
    description: ''
    gen_cmake: 'On'
    gen_linking: dynamic
    generate_options: qt_gui
    hier_block_src_path: '.:'
    id: CommunicationSystem
    max_nouts: '0'
    output_language: python
    placement: (0,0)
    qt_qss_theme: ''
    realtime_scheduling: ''
    run: 'True'
    run_command: '{python} -u {filename}'
    run_options: prompt
    sizing_mode: fixed
    thread_safe_setters: ''
    title: CDP
    window_size: (1000,1000)
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [8, 16.0]
    rotation: 0
    state: enabled

blocks:
- name: access_key
  id: variable
  parameters:
    comment: ''
    value: '''11100001010110101110100010010011'''
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [8, 824.0]
    rotation: 0
    state: enabled
- name: bw
  id: variable
  parameters:
    comment: ''
    value: 500e3
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2048, 1320.0]
    rotation: 0
    state: enabled
- name: delay
  id: variable
  parameters:
    comment: ''
    value: '50'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [96, 920.0]
    rotation: 0
    state: enabled
- name: excess_bw
  id: variable
  parameters:
    comment: ''
    value: '0.35'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [8, 464.0]
    rotation: 0
    state: enabled
- name: freq
  id: variable
  parameters:
    comment: ''
    value: 2.4e9
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2048, 1128.0]
    rotation: 0
    state: enabled
- name: freq_offset
  id: variable
  parameters:
    comment: ''
    value: '0'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [8, 992.0]
    rotation: 0
    state: enabled
- name: hdr_format
  id: variable
  parameters:
    comment: header_format_default
    value: digital.header_format_default(access_key, 0)
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [8, 752.0]
    rotation: 0
    state: enabled
- name: nfilts
  id: variable
  parameters:
    comment: ''
    value: '32'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [88, 608.0]
    rotation: 0
    state: enabled
- name: noise_volt
  id: variable
  parameters:
    comment: ''
    value: '0'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [88, 992.0]
    rotation: 0
    state: enabled
- name: phase_bw
  id: variable
  parameters:
    comment: ''
    value: '0.0628'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [8, 608.0]
    rotation: 0
    state: true
- name: qpsk
  id: variable_constellation_rect
  parameters:
    comment: ''
    const_points: '[0.707+0.707j, -0.707+0.707j, -0.707-0.707j, 0.707-0.707j]'
    imag_sect: '2'
    precision: '8'
    real_sect: '2'
    rot_sym: '4'
    soft_dec_lut: None
    sym_map: '[0, 1, 2, 3]'
    w_imag_sect: '1'
    w_real_sect: '1'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [8, 140.0]
    rotation: 0
    state: enabled
- name: rrc_taps
  id: variable
  parameters:
    comment: ''
    value: firdes.root_raised_cosine(nfilts, nfilts, 1.0/float(sps), 0.35, 11*sps*nfilts)
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [8, 680.0]
    rotation: 0
    state: enabled
- name: samp_rate
  id: variable
  parameters:
    comment: ''
    value: 1.2e6
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2048, 1216.0]
    rotation: 0
    state: enabled
- name: sps
  id: variable
  parameters:
    comment: ''
    value: '4'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [88, 464.0]
    rotation: 0
    state: enabled
- name: thresh
  id: variable
  parameters:
    comment: ''
    value: '1'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [88, 536.0]
    rotation: 0
    state: enabled
- name: time_offset
  id: variable
  parameters:
    comment: ''
    value: '1'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [8, 920.0]
    rotation: 0
    state: enabled
- name: variable_adaptive_algorithm_0
  id: variable_adaptive_algorithm
  parameters:
    comment: ''
    cons: qpsk
    delta: '10.0'
    ffactor: '0.99'
    modulus: '4'
    step_size: '.0001'
    type: cma
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [8, 312.0]
    rotation: 0
    state: true
- name: blocks_repack_bits_bb_1_0_0
  id: blocks_repack_bits_bb
  parameters:
    affinity: ''
    alias: ''
    align_output: 'False'
    comment: MSB
    endianness: gr.GR_MSB_FIRST
    k: '1'
    l: '8'
    len_tag_key: '"packet_len"'
    maxoutbuf: '0'
    minoutbuf: '0'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1064.0, 680]
    rotation: 270
    state: disabled
- name: blocks_repack_bits_bb_1_0_0_0
  id: blocks_repack_bits_bb
  parameters:
    affinity: ''
    alias: ''
    align_output: 'False'
    comment: MSB
    endianness: gr.GR_MSB_FIRST
    k: '1'
    l: '8'
    len_tag_key: '"packet_len"'
    maxoutbuf: '0'
    minoutbuf: '0'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2344.0, 688]
    rotation: 270
    state: disabled
- name: blocks_tag_debug_0
  id: blocks_tag_debug
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    display: 'True'
    filter: '""'
    name: ''
    num_inputs: '1'
    type: complex
    vlen: '1'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1128, 1496.0]
    rotation: 0
    state: disabled
- name: blocks_tagged_stream_mux_0
  id: blocks_tagged_stream_mux
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    lengthtagname: packet_len
    maxoutbuf: '0'
    minoutbuf: '0'
    ninputs: '2'
    tag_preserve_head_pos: '0'
    type: byte
    vlen: '1'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1464.0, 680]
    rotation: 270
    state: disabled
- name: blocks_tagged_stream_mux_0_0
  id: blocks_tagged_stream_mux
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    lengthtagname: packet_len
    maxoutbuf: '0'
    minoutbuf: '0'
    ninputs: '2'
    tag_preserve_head_pos: '0'
    type: byte
    vlen: '1'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2744.0, 688]
    rotation: 270
    state: enabled
- name: blocks_unpack_k_bits_bb_0
  id: blocks_unpack_k_bits_bb
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    k: '2'
    maxoutbuf: '0'
    minoutbuf: '0'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [936.0, 680]
    rotation: 270
    state: disabled
- name: blocks_unpack_k_bits_bb_0_0
  id: blocks_unpack_k_bits_bb
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    k: '2'
    maxoutbuf: '0'
    minoutbuf: '0'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2216.0, 688]
    rotation: 270
    state: disabled
- name: channels_channel_model_0
  id: channels_channel_model
  parameters:
    affinity: ''
    alias: ''
    block_tags: 'False'
    comment: Epsilon is the time offset
    epsilon: time_offset
    freq_offset: freq_offset
    maxoutbuf: '0'
    minoutbuf: '0'
    noise_voltage: noise_volt
    seed: '0'
    taps: '1'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1800, 1456.0]
    rotation: 180
    state: disabled
- name: channels_channel_model_0_0
  id: channels_channel_model
  parameters:
    affinity: ''
    alias: ''
    block_tags: 'False'
    comment: Epsilon is the time offset
    epsilon: time_offset
    freq_offset: freq_offset
    maxoutbuf: '0'
    minoutbuf: '0'
    noise_voltage: noise_volt
    seed: '0'
    taps: '1'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1800, 1600.0]
    rotation: 180
    state: disabled
- name: digital_constellation_decoder_cb_0
  id: digital_constellation_decoder_cb
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    constellation: qpsk
    maxoutbuf: '0'
    minoutbuf: '0'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [936.0, 968]
    rotation: 270
    state: disabled
- name: digital_constellation_decoder_cb_0_0
  id: digital_constellation_decoder_cb
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    constellation: qpsk
    maxoutbuf: '0'
    minoutbuf: '0'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2224.0, 976]
    rotation: 270
    state: disabled
- name: digital_constellation_modulator_0
  id: digital_constellation_modulator
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    constellation: qpsk
    differential: 'True'
    excess_bw: excess_bw
    log: 'False'
    maxoutbuf: '0'
    minoutbuf: '0'
    samples_per_symbol: sps
    truncate: 'False'
    verbose: 'False'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1448.0, 960]
    rotation: 270
    state: disabled
- name: digital_constellation_modulator_0_0
  id: digital_constellation_modulator
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    constellation: qpsk
    differential: 'True'
    excess_bw: excess_bw
    log: 'False'
    maxoutbuf: '0'
    minoutbuf: '0'
    samples_per_symbol: sps
    truncate: 'False'
    verbose: 'False'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2728.0, 968]
    rotation: 270
    state: enabled
- name: digital_correlate_access_code_xx_ts_0
  id: digital_correlate_access_code_xx_ts
  parameters:
    access_code: '"11100001010110101110100010010011"'
    affinity: ''
    alias: ''
    comment: ''
    maxoutbuf: '0'
    minoutbuf: '0'
    tagname: packet_len
    threshold: thresh
    type: byte
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [984.0, 680]
    rotation: 90
    state: disabled
- name: digital_correlate_access_code_xx_ts_0_0
  id: digital_correlate_access_code_xx_ts
  parameters:
    access_code: '"11100001010110101110100010010011"'
    affinity: ''
    alias: ''
    comment: ''
    maxoutbuf: '0'
    minoutbuf: '0'
    tagname: packet_len
    threshold: thresh
    type: byte
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2264.0, 688]
    rotation: 90
    state: disabled
- name: digital_costas_loop_cc_0
  id: digital_costas_loop_cc
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    maxoutbuf: '0'
    minoutbuf: '0'
    order: '4'
    use_snr: 'False'
    w: phase_bw
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [992.0, 992]
    rotation: 90
    state: disabled
- name: digital_costas_loop_cc_0_0
  id: digital_costas_loop_cc
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    maxoutbuf: '0'
    minoutbuf: '0'
    order: '4'
    use_snr: 'False'
    w: phase_bw
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2272.0, 1000]
    rotation: 90
    state: disabled
- name: digital_diff_decoder_bb_0
  id: digital_diff_decoder_bb
  parameters:
    affinity: ''
    alias: ''
    coding: digital.DIFF_DIFFERENTIAL
    comment: ''
    maxoutbuf: '0'
    minoutbuf: '0'
    modulus: '4'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [864.0, 1000]
    rotation: 90
    state: disabled
- name: digital_diff_decoder_bb_0_0
  id: digital_diff_decoder_bb
  parameters:
    affinity: ''
    alias: ''
    coding: digital.DIFF_DIFFERENTIAL
    comment: ''
    maxoutbuf: '0'
    minoutbuf: '0'
    modulus: '4'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2160.0, 1008]
    rotation: 90
    state: disabled
- name: digital_linear_equalizer_0
  id: digital_linear_equalizer
  parameters:
    adapt_after_training: 'True'
    affinity: ''
    alg: variable_adaptive_algorithm_0
    alias: ''
    comment: ''
    maxoutbuf: '0'
    minoutbuf: '0'
    num_taps: '15'
    sps: '2'
    training_sequence: '[ ]'
    training_start_tag: corr_est
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1120.0, 960]
    rotation: 270
    state: disabled
- name: digital_linear_equalizer_0_0
  id: digital_linear_equalizer
  parameters:
    adapt_after_training: 'True'
    affinity: ''
    alg: variable_adaptive_algorithm_0
    alias: ''
    comment: ''
    maxoutbuf: '0'
    minoutbuf: '0'
    num_taps: '15'
    sps: '2'
    training_sequence: '[ ]'
    training_start_tag: corr_est
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2400.0, 968]
    rotation: 270
    state: disabled
- name: digital_map_bb_0
  id: digital_map_bb
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    map: '[0,1,2,3]'
    maxoutbuf: '0'
    minoutbuf: '0'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [888.0, 680]
    rotation: 90
    state: disabled
- name: digital_map_bb_0_0
  id: digital_map_bb
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    map: '[0,1,2,3]'
    maxoutbuf: '0'
    minoutbuf: '0'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2168.0, 688]
    rotation: 90
    state: disabled
- name: digital_pfb_clock_sync_xxx_0
  id: digital_pfb_clock_sync_xxx
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    filter_size: nfilts
    init_phase: nfilts/2
    loop_bw: phase_bw
    max_dev: '1.5'
    maxoutbuf: '0'
    minoutbuf: '0'
    osps: '2'
    sps: sps
    taps: rrc_taps
    type: ccf
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1228.0, 968]
    rotation: 90
    state: disabled
- name: digital_pfb_clock_sync_xxx_0_0
  id: digital_pfb_clock_sync_xxx
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    filter_size: nfilts
    init_phase: nfilts/2
    loop_bw: phase_bw
    max_dev: '1.5'
    maxoutbuf: '0'
    minoutbuf: '0'
    osps: '2'
    sps: sps
    taps: rrc_taps
    type: ccf
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2492.0, 968]
    rotation: 90
    state: disabled
- name: digital_protocol_formatter_async_0
  id: digital_protocol_formatter_async
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    format: hdr_format
    maxoutbuf: '0'
    minoutbuf: '0'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1288.0, 680]
    rotation: 270
    state: disabled
- name: digital_protocol_formatter_async_0_0
  id: digital_protocol_formatter_async
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    format: hdr_format
    maxoutbuf: '0'
    minoutbuf: '0'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2584.0, 688]
    rotation: 270
    state: enabled
- name: epy_block_0
  id: epy_block
  parameters:
    _source_code: "from gnuradio import gr\nimport pmt\nfrom PyQt5 import QtWidgets,\
      \ QtCore\nfrom collections import deque\nfrom datetime import datetime\n\nADDRESS_MAP\
      \ = {\"userA\": 0x00, \"userB\": 0x01, \"userC\": 0x02}\nNAME_MAP = {v: k for\
      \ k, v in ADDRESS_MAP.items()}\n\n\nclass gui_io(gr.basic_block):\n    \"\"\"\
      \n    GUI Chat Window.\n    \"\"\"\n    def __init__(self, my_name=\"userA\"\
      ):\n        gr.basic_block.__init__(self, name=\"Messages App\", in_sig=[],\
      \ out_sig=[])\n        self.my_name = my_name\n        self.tx_queue = deque()\n\
      \n        self.message_port_register_in(pmt.intern(\"in\"))\n        self.set_msg_handler(pmt.intern(\"\
      in\"), self.handle_rx)\n        self.message_port_register_out(pmt.intern(\"\
      out\"))\n\n        self.app = QtWidgets.QApplication.instance() or QtWidgets.QApplication([])\n\
      \        self.window = ChatWindow(self, self.tx_queue)\n        self.window.show()\n\
      \n        self.timer = QtCore.QTimer()\n        self.timer.timeout.connect(self.process_tx)\n\
      \        self.timer.start(50)\n\n    def handle_rx(self, msg_pmt):\n       \
      \ if pmt.is_pair(msg_pmt):\n            data = list(pmt.u8vector_elements(pmt.cdr(msg_pmt)))\n\
      \            if len(data) < 3:\n                return\n            emergency\
      \ = bool(data[0])\n            src_addr = data[1]\n            dst_addr = data[2]\n\
      \            msg_bytes = data[3:]\n            msg_str = bytes(msg_bytes).decode('utf-8',\
      \ errors='ignore')\n            src_name = NAME_MAP.get(src_addr, f\"Unknown({src_addr})\"\
      )\n            dst_name = NAME_MAP.get(dst_addr, f\"Unknown({dst_addr})\")\n\
      \n            self.window.new_message_signal.emit(msg_str, emergency, src_name,\
      \ dst_name)\n\n    def process_tx(self):\n        while self.tx_queue:\n   \
      \         msg, emergency, src, dst = self.tx_queue.popleft()\n            dst_addr\
      \ = ADDRESS_MAP.get(dst, 0xFF)\n            data_list = [int(emergency), ADDRESS_MAP[self.my_name],\
      \ dst_addr] + list(msg.encode('utf-8'))\n            out_pdu = pmt.cons(pmt.make_dict(),\
      \ pmt.init_u8vector(len(data_list), data_list))\n            self.message_port_pub(pmt.intern(\"\
      out\"), out_pdu)\n\n    def stop(self):\n        self.window.close()\n     \
      \   return True\n\n\nclass ChatWindow(QtWidgets.QWidget):\n    new_message_signal\
      \ = QtCore.pyqtSignal(str, bool, str, str)\n\n    def __init__(self, block,\
      \ tx_queue):\n        super().__init__()\n        self.block = block\n     \
      \   self.tx_queue = tx_queue\n\n        self.new_message_signal.connect(self.add_message_bubble)\n\
      \n        self.setWindowTitle(f\"Messages: {self.block.my_name}\")\n       \
      \ self.setStyleSheet(\"background-color: #e6f0f8;\")\n        self.resize(700,\
      \ 500)\n\n        self.layout = QtWidgets.QVBoxLayout(self)\n        self.layout.setContentsMargins(15,\
      \ 15, 15, 15)\n        self.layout.setSpacing(10)\n\n        self.scroll_area\
      \ = QtWidgets.QScrollArea()\n        self.scroll_area.setWidgetResizable(True)\n\
      \        self.scroll_area.setStyleSheet(\"\"\"\n            QScrollArea { \n\
      \                border: 1px solid #c0c0c0; \n                border-radius:\
      \ 8px; \n                background-color: #ffffff;\n            }\n       \
      \ \"\"\")\n        self.chat_container = QtWidgets.QWidget()\n        self.chat_container.setStyleSheet(\"\
      background-color: #ffffff; border-radius: 8px;\")\n        self.chat_layout\
      \ = QtWidgets.QVBoxLayout(self.chat_container)\n        self.chat_layout.setContentsMargins(10,\
      \ 10, 10, 10)\n        self.chat_layout.setSpacing(8)\n        self.chat_layout.addStretch(1)\n\
      \        self.scroll_area.setWidget(self.chat_container)\n        self.layout.addWidget(self.scroll_area)\n\
      \n        input_group = QtWidgets.QFrame()\n        input_group.setStyleSheet(\"\
      \"\"\n            QFrame { \n                background-color: #f0f4f8; \n \
      \               border-radius: 8px; \n            }\n        \"\"\")\n     \
      \   input_layout = QtWidgets.QHBoxLayout(input_group)\n        input_layout.setContentsMargins(8,\
      \ 5, 8, 5)\n        input_layout.setSpacing(5)\n\n        self.input_line =\
      \ QtWidgets.QLineEdit()\n        self.input_line.setPlaceholderText(\"Text Message\
      \ . \")\n        self.input_line.setStyleSheet(\"padding: 5px; font-size: 12pt;\"\
      )\n        input_layout.addWidget(self.input_line, 3)\n\n        self.dst_line\
      \ = QtWidgets.QLineEdit()\n        self.dst_line.setPlaceholderText(\"To\")\n\
      \        self.dst_line.setFixedWidth(120)\n        self.dst_line.setStyleSheet(\"\
      padding: 5px; font-size: 12pt;\")\n        input_layout.addWidget(self.dst_line,\
      \ 1)\n\n        self.emergency_checkbox = QtWidgets.QCheckBox(\"Emergency\"\
      )\n        self.emergency_checkbox.setStyleSheet(\"font-size: 11pt; padding:\
      \ 3px;\")\n        input_layout.addWidget(self.emergency_checkbox)\n\n     \
      \   self.send_button = QtWidgets.QPushButton(\"Send\")\n        self.send_button.setStyleSheet(\"\
      \"\"\n            QPushButton { \n                background-color: #4a90e2;\
      \ \n                color: white; \n                font-size: 12pt; \n    \
      \            padding: 6px 12px; \n                border-radius: 6px; \n   \
      \         }\n            QPushButton:hover { background-color: #357ab7; }\n\
      \        \"\"\")\n        input_layout.addWidget(self.send_button)\n\n     \
      \   self.layout.addWidget(input_group)\n\n        self.send_button.clicked.connect(self.queue_message)\n\
      \        self.input_line.returnPressed.connect(self.queue_message)\n\n    def\
      \ queue_message(self):\n        msg = self.input_line.text()\n        dst_name\
      \ = self.dst_line.text()\n        if msg and dst_name in ADDRESS_MAP:\n    \
      \        emergency_flag = self.emergency_checkbox.isChecked()\n            self.tx_queue.append((msg,\
      \ emergency_flag, self.block.my_name, dst_name))\n            self.new_message_signal.emit(msg,\
      \ emergency_flag, self.block.my_name, dst_name)\n            self.input_line.clear()\n\
      \            self.dst_line.clear()\n            self.emergency_checkbox.setChecked(False)\n\
      \n    def add_message_bubble(self, msg, emergency, src, dst):\n        bubble\
      \ = QtWidgets.QFrame()\n        bubble.setMaximumWidth(400)\n        bubble_layout\
      \ = QtWidgets.QVBoxLayout(bubble)\n        bubble_layout.setContentsMargins(12,\
      \ 8, 12, 8)\n        bubble_layout.setSpacing(2)\n\n        msg_label = QtWidgets.QLabel(msg)\n\
      \        msg_label.setWordWrap(True)\n        msg_label.setStyleSheet(\"font-size:\
      \ 12pt;\")\n        bubble_layout.addWidget(msg_label)\n\n        other_name\
      \ = dst if src == self.block.my_name else src\n        timestamp = datetime.now().strftime(\"\
      %H:%M:%S\")\n        footer_label = QtWidgets.QLabel(f\"{other_name} | {timestamp}\"\
      )\n        footer_label.setStyleSheet(\"font-size: 8pt; color: #555555;\")\n\
      \        bubble_layout.addWidget(footer_label)\n\n        if emergency:\n  \
      \          bubble.setStyleSheet(\"background-color: #ffcccc; border-radius:\
      \ 10px;\")\n        else:\n            if src == self.block.my_name:\n     \
      \           bubble.setStyleSheet(\"background-color: #cce5ff; border-radius:\
      \ 10px;\")\n            else:\n                bubble.setStyleSheet(\"background-color:\
      \ #d9f2d9; border-radius: 10px;\")\n\n        h_layout = QtWidgets.QHBoxLayout()\n\
      \        if src == self.block.my_name:\n            h_layout.addStretch()\n\
      \            h_layout.addWidget(bubble)\n        else:\n            h_layout.addWidget(bubble)\n\
      \            h_layout.addStretch()\n\n        self.chat_layout.insertLayout(self.chat_layout.count()-1,\
      \ h_layout)\n        QtCore.QTimer.singleShot(0, lambda: self.scroll_area.verticalScrollBar().setValue(\n\
      \            self.scroll_area.verticalScrollBar().maximum()))"
    affinity: ''
    alias: ''
    comment: ''
    maxoutbuf: '0'
    minoutbuf: '0'
    my_name: '"userA"'
  states:
    _io_cache: ('Messages App', 'gui_io', [('my_name', "'userA'")], [('in', 'message',
      1)], [('out', 'message', 1)], '\n    GUI Chat Window.\n    ', ['my_name'])
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1136, 120.0]
    rotation: 0
    state: disabled
- name: epy_block_0_0
  id: epy_block
  parameters:
    _source_code: "from gnuradio import gr\nimport pmt\nfrom PyQt5 import QtWidgets,\
      \ QtCore\nfrom collections import deque\nfrom datetime import datetime\n\nADDRESS_MAP\
      \ = {\"userA\": 0x00, \"userB\": 0x01, \"userC\": 0x02}\nNAME_MAP = {v: k for\
      \ k, v in ADDRESS_MAP.items()}\n\n\nclass gui_io(gr.basic_block):\n    \"\"\"\
      \n    GUI I/O block with PyQt5 chat window (bubble style)\n    \"\"\"\n    def\
      \ __init__(self, my_name=\"userA\"):\n        gr.basic_block.__init__(self,\
      \ name=\"gui_io\", in_sig=[], out_sig=[])\n        self.my_name = my_name\n\n\
      \        # Queue for TX\n        self.tx_queue = deque()\n\n        # Message\
      \ ports\n        self.message_port_register_in(pmt.intern(\"in\"))\n       \
      \ self.set_msg_handler(pmt.intern(\"in\"), self.handle_rx)\n        self.message_port_register_out(pmt.intern(\"\
      out\"))\n\n        # Start GUI\n        self.app = QtWidgets.QApplication.instance()\
      \ or QtWidgets.QApplication([])\n        self.window = ChatWindow(self, self.tx_queue)\n\
      \        self.window.show()\n\n        # Timer for TX messages\n        self.timer\
      \ = QtCore.QTimer()\n        self.timer.timeout.connect(self.process_tx)\n \
      \       self.timer.start(50)\n\n    def handle_rx(self, msg_pmt):\n        if\
      \ pmt.is_pair(msg_pmt):\n            data = list(pmt.u8vector_elements(pmt.cdr(msg_pmt)))\n\
      \            if len(data) < 3:\n                return\n            emergency\
      \ = bool(data[0])\n            src_addr = data[1]\n            dst_addr = data[2]\n\
      \            msg_bytes = data[3:]\n            msg_str = bytes(msg_bytes).decode('utf-8',\
      \ errors='ignore')\n            src_name = NAME_MAP.get(src_addr, f\"Unknown({src_addr})\"\
      )\n            dst_name = NAME_MAP.get(dst_addr, f\"Unknown({dst_addr})\")\n\
      \n            # Thread-safe emit to GUI\n            self.window.new_message_signal.emit(msg_str,\
      \ emergency, src_name, dst_name)\n\n    def process_tx(self):\n        while\
      \ self.tx_queue:\n            msg, emergency, src, dst = self.tx_queue.popleft()\n\
      \            dst_addr = ADDRESS_MAP.get(dst, 0xFF)\n            data_list =\
      \ [int(emergency), ADDRESS_MAP[self.my_name], dst_addr] + list(msg.encode('utf-8'))\n\
      \            out_pdu = pmt.cons(pmt.make_dict(), pmt.init_u8vector(len(data_list),\
      \ data_list))\n            self.message_port_pub(pmt.intern(\"out\"), out_pdu)\n\
      \n    def stop(self):\n        self.window.close()\n        return True\n\n\n\
      class ChatWindow(QtWidgets.QWidget):\n    new_message_signal = QtCore.pyqtSignal(str,\
      \ bool, str, str)\n\n    def __init__(self, block, tx_queue):\n        super().__init__()\n\
      \        self.block = block\n        self.tx_queue = tx_queue\n\n        # Connect\
      \ signal\n        self.new_message_signal.connect(self.add_message_bubble)\n\
      \n        self.setWindowTitle(f\"Messages: {self.block.my_name}\")\n       \
      \ self.setStyleSheet(\"background-color: #e6f0f8;\")\n        self.resize(700,\
      \ 500)\n\n        # Main layout\n        self.layout = QtWidgets.QVBoxLayout(self)\n\
      \        self.layout.setContentsMargins(15, 15, 15, 15)\n        self.layout.setSpacing(10)\n\
      \n        # Scroll area for chat messages\n        self.scroll_area = QtWidgets.QScrollArea()\n\
      \        self.scroll_area.setWidgetResizable(True)\n        self.scroll_area.setStyleSheet(\"\
      \"\"\n            QScrollArea { \n                border: 1px solid #c0c0c0;\
      \ \n                border-radius: 8px; \n                background-color:\
      \ #ffffff;\n            }\n        \"\"\")\n        self.chat_container = QtWidgets.QWidget()\n\
      \        self.chat_container.setStyleSheet(\"background-color: #ffffff; border-radius:\
      \ 8px;\")\n        self.chat_layout = QtWidgets.QVBoxLayout(self.chat_container)\n\
      \        self.chat_layout.setContentsMargins(10, 10, 10, 10)\n        self.chat_layout.setSpacing(8)\n\
      \        self.chat_layout.addStretch(1)\n        self.scroll_area.setWidget(self.chat_container)\n\
      \        self.layout.addWidget(self.scroll_area)\n\n        # Input area\n \
      \       input_group = QtWidgets.QFrame()\n        input_group.setStyleSheet(\"\
      \"\"\n            QFrame { \n                background-color: #f0f4f8; \n \
      \               border-radius: 8px; \n            }\n        \"\"\")\n     \
      \   input_layout = QtWidgets.QHBoxLayout(input_group)\n        input_layout.setContentsMargins(8,\
      \ 5, 8, 5)\n        input_layout.setSpacing(5)\n\n        self.input_line =\
      \ QtWidgets.QLineEdit()\n        self.input_line.setPlaceholderText(\"Text Message\
      \ . \")\n        self.input_line.setStyleSheet(\"padding: 5px; font-size: 12pt;\"\
      )\n        input_layout.addWidget(self.input_line, 3)\n\n        self.dst_line\
      \ = QtWidgets.QLineEdit()\n        self.dst_line.setPlaceholderText(\"To\")\n\
      \        self.dst_line.setFixedWidth(120)\n        self.dst_line.setStyleSheet(\"\
      padding: 5px; font-size: 12pt;\")\n        input_layout.addWidget(self.dst_line,\
      \ 1)\n\n        self.emergency_checkbox = QtWidgets.QCheckBox(\"Emergency\"\
      )\n        self.emergency_checkbox.setStyleSheet(\"font-size: 11pt; padding:\
      \ 3px;\")\n        input_layout.addWidget(self.emergency_checkbox)\n\n     \
      \   self.send_button = QtWidgets.QPushButton(\"Send\")\n        self.send_button.setStyleSheet(\"\
      \"\"\n            QPushButton { \n                background-color: #4a90e2;\
      \ \n                color: white; \n                font-size: 12pt; \n    \
      \            padding: 6px 12px; \n                border-radius: 6px; \n   \
      \         }\n            QPushButton:hover { background-color: #357ab7; }\n\
      \        \"\"\")\n        input_layout.addWidget(self.send_button)\n\n     \
      \   self.layout.addWidget(input_group)\n\n        # Signals\n        self.send_button.clicked.connect(self.queue_message)\n\
      \        self.input_line.returnPressed.connect(self.queue_message)\n\n    def\
      \ queue_message(self):\n        msg = self.input_line.text()\n        dst_name\
      \ = self.dst_line.text()\n        if msg and dst_name in ADDRESS_MAP:\n    \
      \        emergency_flag = self.emergency_checkbox.isChecked()\n            #\
      \ Append to TX queue\n            self.tx_queue.append((msg, emergency_flag,\
      \ self.block.my_name, dst_name))\n            # Display own message immediately\n\
      \            self.new_message_signal.emit(msg, emergency_flag, self.block.my_name,\
      \ dst_name)\n            self.input_line.clear()\n            self.dst_line.clear()\n\
      \            self.emergency_checkbox.setChecked(False)\n\n    def add_message_bubble(self,\
      \ msg, emergency, src, dst):\n        bubble = QtWidgets.QFrame()\n        bubble.setMaximumWidth(400)\n\
      \        bubble_layout = QtWidgets.QVBoxLayout(bubble)\n        bubble_layout.setContentsMargins(12,\
      \ 8, 12, 8)\n        bubble_layout.setSpacing(2)\n\n        # Message text\n\
      \        msg_label = QtWidgets.QLabel(msg)\n        msg_label.setWordWrap(True)\n\
      \        msg_label.setStyleSheet(\"font-size: 12pt;\")\n        bubble_layout.addWidget(msg_label)\n\
      \n        # Footer: timestamp + other party\n        other_name = dst if src\
      \ == self.block.my_name else src\n        timestamp = datetime.now().strftime(\"\
      %H:%M:%S\")\n        footer_label = QtWidgets.QLabel(f\"{other_name} | {timestamp}\"\
      )\n        footer_label.setStyleSheet(\"font-size: 8pt; color: #555555;\")\n\
      \        bubble_layout.addWidget(footer_label)\n\n        # Bubble color\n \
      \       if emergency:\n            bubble.setStyleSheet(\"background-color:\
      \ #ffcccc; border-radius: 10px;\")\n        else:\n            if src == self.block.my_name:\n\
      \                bubble.setStyleSheet(\"background-color: #cce5ff; border-radius:\
      \ 10px;\")\n            else:\n                bubble.setStyleSheet(\"background-color:\
      \ #d9f2d9; border-radius: 10px;\")\n\n        # Alignment\n        h_layout\
      \ = QtWidgets.QHBoxLayout()\n        if src == self.block.my_name:\n       \
      \     h_layout.addStretch()\n            h_layout.addWidget(bubble)\n      \
      \  else:\n            h_layout.addWidget(bubble)\n            h_layout.addStretch()\n\
      \n        self.chat_layout.insertLayout(self.chat_layout.count()-1, h_layout)\n\
      \        QtCore.QTimer.singleShot(0, lambda: self.scroll_area.verticalScrollBar().setValue(\n\
      \            self.scroll_area.verticalScrollBar().maximum()))"
    affinity: ''
    alias: ''
    comment: ''
    maxoutbuf: '0'
    minoutbuf: '0'
    my_name: '"userB"'
  states:
    _io_cache: ('gui_io', 'gui_io', [('my_name', "'userA'")], [('in', 'message', 1)],
      [('out', 'message', 1)], '\n    GUI I/O block with PyQt5 chat window (bubble
      style)\n    ', ['my_name'])
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2416, 128.0]
    rotation: 0
    state: enabled
- name: epy_block_1
  id: epy_block
  parameters:
    _source_code: "from gnuradio import gr\nimport pmt\nfrom Crypto.Cipher import\
      \ AES\nfrom Crypto.Util.Padding import pad, unpad\nimport threading\nfrom collections\
      \ import deque\n\nclass aes128_block(gr.basic_block):\n    \"\"\"\n    AES-128\
      \ Encrypt/Decrypt.\n\n    Input TX:  PDU = pmt.cons(meta, data)\n        meta:\
      \ empty\n        data: pmt.u8vector [emergency(1B), src_addr(1B), dst_addr(1B),\
      \ plaintext message bytes]\n    Output TX: PDU with encrypted payload (IV +\
      \ ciphertext), first 3 bytes unchanged\n\n    Input RX:  PDU with encrypted\
      \ payload\n    Output RX: PDU with decrypted payload, first 3 bytes unchanged\n\
      \    \"\"\"\n\n    def __init__(self, key=b'0123456789abcdef'):\n        gr.basic_block.__init__(self,\
      \ name=\"Encrypt/Decrypt.\", in_sig=[], out_sig=[])\n\n        self.message_port_register_in(pmt.intern(\"\
      in_tx\"))\n        self.set_msg_handler(pmt.intern(\"in_tx\"), self.handle_tx)\n\
      \        self.message_port_register_in(pmt.intern(\"in_rx\"))\n        self.set_msg_handler(pmt.intern(\"\
      in_rx\"), self.handle_rx)\n        self.message_port_register_out(pmt.intern(\"\
      out_tx\"))\n        self.message_port_register_out(pmt.intern(\"out_rx\"))\n\
      \n        if len(key) != 16:\n            raise ValueError(\"AES-128 requires\
      \ 16-byte key\")\n        self.key = key\n\n        self.tx_queue = deque()\n\
      \        self.rx_queue = deque()\n\n        self.running = True\n        self.tx_thread\
      \ = threading.Thread(target=self.tx_loop, daemon=True)\n        self.rx_thread\
      \ = threading.Thread(target=self.rx_loop, daemon=True)\n        self.tx_thread.start()\n\
      \        self.rx_thread.start()\n\n    def handle_tx(self, msg_pmt):\n     \
      \   if pmt.is_pair(msg_pmt):\n            self.tx_queue.append(msg_pmt)\n\n\
      \    def handle_rx(self, msg_pmt):\n        if pmt.is_pair(msg_pmt):\n     \
      \       self.rx_queue.append(msg_pmt)\n\n    def tx_loop(self):\n        while\
      \ self.running:\n            if self.tx_queue:\n                msg_pdu = self.tx_queue.popleft()\n\
      \                data_pmt = pmt.cdr(msg_pdu)\n                if pmt.is_u8vector(data_pmt):\n\
      \                    data_bytes = list(pmt.u8vector_elements(data_pmt))\n  \
      \                  if len(data_bytes) < 3:\n                        continue\n\
      \                    header = data_bytes[:3]\n                    payload =\
      \ data_bytes[3:]\n\n                    cipher = AES.new(self.key, AES.MODE_CBC)\n\
      \                    ct_bytes = cipher.iv + cipher.encrypt(pad(bytes(payload),\
      \ AES.block_size))\n\n                    out_bytes = header + list(ct_bytes)\n\
      \                    out_pmt = pmt.init_u8vector(len(out_bytes), out_bytes)\n\
      \                    out_pdu = pmt.cons(pmt.make_dict(), out_pmt)\n        \
      \            self.message_port_pub(pmt.intern(\"out_tx\"), out_pdu)\n      \
      \      else:\n                threading.Event().wait(0.01)\n\n    def rx_loop(self):\n\
      \        while self.running:\n            if self.rx_queue:\n              \
      \  msg_pdu = self.rx_queue.popleft()\n                data_pmt = pmt.cdr(msg_pdu)\n\
      \                if pmt.is_u8vector(data_pmt):\n                    data_bytes\
      \ = list(pmt.u8vector_elements(data_pmt))\n                    if len(data_bytes)\
      \ < 3 + 16:  # header + IV\n                        continue\n             \
      \       header = data_bytes[:3]\n                    ct_bytes = bytes(data_bytes[3:])\n\
      \                    iv = ct_bytes[:16]\n                    ct = ct_bytes[16:]\n\
      \                    cipher = AES.new(self.key, AES.MODE_CBC, iv=iv)\n     \
      \               try:\n                        pt_bytes = unpad(cipher.decrypt(ct),\
      \ AES.block_size)\n                        out_bytes = header + list(pt_bytes)\n\
      \                        out_pmt = pmt.init_u8vector(len(out_bytes), out_bytes)\n\
      \                        out_pdu = pmt.cons(pmt.make_dict(), out_pmt)\n    \
      \                    self.message_port_pub(pmt.intern(\"out_rx\"), out_pdu)\n\
      \                    except ValueError:\n                        continue\n\
      \            else:\n                threading.Event().wait(0.01)\n\n    def\
      \ stop(self):\n        self.running = False\n        self.tx_thread.join()\n\
      \        self.rx_thread.join()\n        return True"
    affinity: ''
    alias: ''
    comment: ''
    key: 'b''mysecretkey12345'' '
    maxoutbuf: '0'
    minoutbuf: '0'
  states:
    _io_cache: '(''Encrypt/Decrypt.'', ''aes128_block'', [(''key'', "b''0123456789abcdef''")],
      [(''in_rx'', ''message'', 1), (''in_tx'', ''message'', 1)], [(''out_rx'', ''message'',
      1), (''out_tx'', ''message'', 1)], ''\n    AES-128 Encrypt/Decrypt.\n\n    Input
      TX:  PDU = pmt.cons(meta, data)\n        meta: empty\n        data: pmt.u8vector
      [emergency(1B), src_addr(1B), dst_addr(1B), plaintext message bytes]\n    Output
      TX: PDU with encrypted payload (IV + ciphertext), first 3 bytes unchanged\n\n    Input
      RX:  PDU with encrypted payload\n    Output RX: PDU with decrypted payload,
      first 3 bytes unchanged\n    '', [''key''])'
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [872, 184.0]
    rotation: 180
    state: disabled
- name: epy_block_1_0
  id: epy_block
  parameters:
    _source_code: "from gnuradio import gr\nimport pmt\nfrom Crypto.Cipher import\
      \ AES\nfrom Crypto.Util.Padding import pad, unpad\nimport threading\nfrom collections\
      \ import deque\n\nclass aes128_block(gr.basic_block):\n    \"\"\"\n    AES-128\
      \ Encrypt/Decrypt Block (PDU-based, Payload Only)\n    -------------------------------------------------------\n\
      \    Input TX:  PDU = pmt.cons(meta, data)\n        meta: empty\n        data:\
      \ pmt.u8vector [emergency(1B), src_addr(1B), dst_addr(1B), plaintext message\
      \ bytes]\n    Output TX: PDU with encrypted payload (IV + ciphertext), first\
      \ 3 bytes unchanged\n\n    Input RX:  PDU with encrypted payload\n    Output\
      \ RX: PDU with decrypted payload, first 3 bytes unchanged\n    \"\"\"\n\n  \
      \  def __init__(self, key=b'0123456789abcdef'):\n        gr.basic_block.__init__(self,\
      \ name=\"AES-128\", in_sig=[], out_sig=[])\n\n        # Ports\n        self.message_port_register_in(pmt.intern(\"\
      in_tx\"))\n        self.set_msg_handler(pmt.intern(\"in_tx\"), self.handle_tx)\n\
      \        self.message_port_register_in(pmt.intern(\"in_rx\"))\n        self.set_msg_handler(pmt.intern(\"\
      in_rx\"), self.handle_rx)\n        self.message_port_register_out(pmt.intern(\"\
      out_tx\"))\n        self.message_port_register_out(pmt.intern(\"out_rx\"))\n\
      \n        # AES key\n        if len(key) != 16:\n            raise ValueError(\"\
      AES-128 requires 16-byte key\")\n        self.key = key\n\n        # Queues\n\
      \        self.tx_queue = deque()\n        self.rx_queue = deque()\n\n      \
      \  # Threads\n        self.running = True\n        self.tx_thread = threading.Thread(target=self.tx_loop,\
      \ daemon=True)\n        self.rx_thread = threading.Thread(target=self.rx_loop,\
      \ daemon=True)\n        self.tx_thread.start()\n        self.rx_thread.start()\n\
      \n    # ---------------- Handlers ----------------\n    def handle_tx(self,\
      \ msg_pmt):\n        if pmt.is_pair(msg_pmt):\n            self.tx_queue.append(msg_pmt)\n\
      \n    def handle_rx(self, msg_pmt):\n        if pmt.is_pair(msg_pmt):\n    \
      \        self.rx_queue.append(msg_pmt)\n\n    # ---------------- TX loop ----------------\n\
      \    def tx_loop(self):\n        while self.running:\n            if self.tx_queue:\n\
      \                msg_pdu = self.tx_queue.popleft()\n                data_pmt\
      \ = pmt.cdr(msg_pdu)\n                if pmt.is_u8vector(data_pmt):\n      \
      \              data_bytes = list(pmt.u8vector_elements(data_pmt))\n        \
      \            if len(data_bytes) < 3:\n                        continue\n   \
      \                 header = data_bytes[:3]  # emergency, src_addr, dst_addr\n\
      \                    payload = data_bytes[3:]\n\n                    # Encrypt\
      \ payload only\n                    cipher = AES.new(self.key, AES.MODE_CBC)\n\
      \                    ct_bytes = cipher.iv + cipher.encrypt(pad(bytes(payload),\
      \ AES.block_size))\n\n                    # Output PDU: header + encrypted payload\n\
      \                    out_bytes = header + list(ct_bytes)\n                 \
      \   out_pmt = pmt.init_u8vector(len(out_bytes), out_bytes)\n               \
      \     out_pdu = pmt.cons(pmt.make_dict(), out_pmt)\n                    self.message_port_pub(pmt.intern(\"\
      out_tx\"), out_pdu)\n            else:\n                threading.Event().wait(0.01)\n\
      \n    # ---------------- RX loop ----------------\n    def rx_loop(self):\n\
      \        while self.running:\n            if self.rx_queue:\n              \
      \  msg_pdu = self.rx_queue.popleft()\n                data_pmt = pmt.cdr(msg_pdu)\n\
      \                if pmt.is_u8vector(data_pmt):\n                    data_bytes\
      \ = list(pmt.u8vector_elements(data_pmt))\n                    if len(data_bytes)\
      \ < 3 + 16:  # header + IV\n                        continue\n             \
      \       header = data_bytes[:3]\n                    ct_bytes = bytes(data_bytes[3:])\n\
      \                    iv = ct_bytes[:16]\n                    ct = ct_bytes[16:]\n\
      \                    cipher = AES.new(self.key, AES.MODE_CBC, iv=iv)\n     \
      \               try:\n                        pt_bytes = unpad(cipher.decrypt(ct),\
      \ AES.block_size)\n                        out_bytes = header + list(pt_bytes)\n\
      \                        out_pmt = pmt.init_u8vector(len(out_bytes), out_bytes)\n\
      \                        out_pdu = pmt.cons(pmt.make_dict(), out_pmt)\n    \
      \                    self.message_port_pub(pmt.intern(\"out_rx\"), out_pdu)\n\
      \                    except ValueError:\n                        continue\n\
      \            else:\n                threading.Event().wait(0.01)\n\n    # ----------------\
      \ Stop ----------------\n    def stop(self):\n        self.running = False\n\
      \        self.tx_thread.join()\n        self.rx_thread.join()\n        return\
      \ True"
    affinity: ''
    alias: ''
    comment: ''
    key: 'b''mysecretkey12345'' '
    maxoutbuf: '0'
    minoutbuf: '0'
  states:
    _io_cache: '(''AES-128'', ''aes128_block'', [(''key'', "b''0123456789abcdef''")],
      [(''in_rx'', ''message'', 1), (''in_tx'', ''message'', 1)], [(''out_rx'', ''message'',
      1), (''out_tx'', ''message'', 1)], ''\n    AES-128 Encrypt/Decrypt Block (PDU-based,
      Payload Only)\n    -------------------------------------------------------\n    Input
      TX:  PDU = pmt.cons(meta, data)\n        meta: empty\n        data: pmt.u8vector
      [emergency(1B), src_addr(1B), dst_addr(1B), plaintext message bytes]\n    Output
      TX: PDU with encrypted payload (IV + ciphertext), first 3 bytes unchanged\n\n    Input
      RX:  PDU with encrypted payload\n    Output RX: PDU with decrypted payload,
      first 3 bytes unchanged\n    '', [''key''])'
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2400, 184.0]
    rotation: 180
    state: enabled
- name: epy_block_2
  id: epy_block
  parameters:
    _source_code: "from gnuradio import gr\nimport pmt\nimport threading\nfrom collections\
      \ import deque\nimport time\n\nclass msgQueue(gr.basic_block):\n    \"\"\"\n\
      \    Message Queue Block with Priority Handling.\n    PDU format:\n        meta:\
      \ empty\n        data (pmt.u8vector):\n            [emergency(1B), src_addr(1B),\
      \ dst_addr(1B), message payload bytes]\n    \"\"\"\n\n    def __init__(self):\n\
      \        gr.basic_block.__init__(self, name=\"MsgQueue\", in_sig=[], out_sig=[])\n\
      \n        self.message_port_register_in(pmt.intern(\"in_tx\"))\n        self.set_msg_handler(pmt.intern(\"\
      in_tx\"), self.handle_in_tx)\n        self.message_port_register_in(pmt.intern(\"\
      in_rx\"))\n        self.set_msg_handler(pmt.intern(\"in_rx\"), self.handle_in_rx)\n\
      \n        self.message_port_register_out(pmt.intern(\"out_tx\"))\n        self.message_port_register_out(pmt.intern(\"\
      out_rx\"))\n\n        self.tx_emergency = deque()\n        self.tx_normal =\
      \ deque()\n        self.rx_emergency = deque()\n        self.rx_normal = deque()\n\
      \n        self.running = True\n        self.tx_thread = threading.Thread(target=self.tx_loop,\
      \ daemon=True)\n        self.rx_thread = threading.Thread(target=self.rx_loop,\
      \ daemon=True)\n        self.tx_thread.start()\n        self.rx_thread.start()\n\
      \n    def handle_in_tx(self, msg_pmt):\n        if pmt.is_pair(msg_pmt):\n \
      \           data_pmt = pmt.cdr(msg_pmt)\n            if pmt.is_u8vector(data_pmt)\
      \ and len(pmt.u8vector_elements(data_pmt)) >= 3:\n                emergency_flag\
      \ = bool(pmt.u8vector_elements(data_pmt)[0])\n                if emergency_flag:\n\
      \                    self.tx_emergency.append(msg_pmt)\n                else:\n\
      \                    self.tx_normal.append(msg_pmt)\n\n    def handle_in_rx(self,\
      \ msg_pmt):\n        if pmt.is_pair(msg_pmt):\n            data_pmt = pmt.cdr(msg_pmt)\n\
      \            if pmt.is_u8vector(data_pmt) and len(pmt.u8vector_elements(data_pmt))\
      \ >= 3:\n                emergency_flag = bool(pmt.u8vector_elements(data_pmt)[0])\n\
      \                if emergency_flag:\n                    self.rx_emergency.append(msg_pmt)\n\
      \                else:\n                    self.rx_normal.append(msg_pmt)\n\
      \n    def tx_loop(self):\n        while self.running:\n            msg = None\n\
      \            if self.tx_emergency:\n                msg = self.tx_emergency.popleft()\n\
      \            elif self.tx_normal:\n                msg = self.tx_normal.popleft()\n\
      \n            if msg is not None:\n                out_pdu = pmt.cons(pmt.make_dict(),\
      \ pmt.cdr(msg))\n                self.message_port_pub(pmt.intern(\"out_tx\"\
      ), out_pdu)\n            else:\n                time.sleep(0.01)\n\n    def\
      \ rx_loop(self):\n        while self.running:\n            msg = None\n    \
      \        if self.rx_emergency:\n                msg = self.rx_emergency.popleft()\n\
      \            elif self.rx_normal:\n                msg = self.rx_normal.popleft()\n\
      \n            if msg is not None:\n                out_pdu = pmt.cons(pmt.make_dict(),\
      \ pmt.cdr(msg))\n                self.message_port_pub(pmt.intern(\"out_rx\"\
      ), out_pdu)\n            else:\n                time.sleep(0.01)\n\n    def\
      \ stop(self):\n        self.running = False\n        self.tx_thread.join()\n\
      \        self.rx_thread.join()\n        return True"
    affinity: ''
    alias: ''
    comment: ''
    maxoutbuf: '0'
    minoutbuf: '0'
  states:
    _io_cache: '(''MsgQueue'', ''msgQueue'', [], [(''in_rx'', ''message'', 1), (''in_tx'',
      ''message'', 1)], [(''out_rx'', ''message'', 1), (''out_tx'', ''message'', 1)],
      ''\n    Message Queue Block with Priority Handling.\n    PDU format:\n        meta:
      empty\n        data (pmt.u8vector):\n            [emergency(1B), src_addr(1B),
      dst_addr(1B), message payload bytes]\n    '', [])'
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1144, 248.0]
    rotation: 0
    state: disabled
- name: epy_block_22_0
  id: epy_block
  parameters:
    _source_code: "from gnuradio import gr\nimport pmt\nfrom PyQt5 import QtWidgets,\
      \ QtCore\nimport sys\n\nclass pdu_probe_qt(gr.basic_block):\n    \"\"\"\n  \
      \  PDU unit8 Probe.\n    \"\"\"\n    def __init__(self):\n        gr.basic_block.__init__(self,\n\
      \            name=\"PDU-u8 Probe\",\n            in_sig=[],\n            out_sig=[]\n\
      \        )\n        self.message_port_register_in(pmt.intern(\"in\"))\n    \
      \    self.set_msg_handler(pmt.intern(\"in\"), self.handle_rx)\n        self.pdu_queue\
      \ = []\n\n        self.app = QtWidgets.QApplication.instance() or QtWidgets.QApplication(sys.argv)\n\
      \        self.window = QtWidgets.QWidget()\n        self.window.setWindowTitle(\"\
      PDU Probe\")\n        self.window.resize(800, 600)\n        self.layout = QtWidgets.QVBoxLayout(self.window)\n\
      \        self.display = QtWidgets.QTextEdit()\n        self.display.setReadOnly(True)\n\
      \        self.layout.addWidget(self.display)\n        self.window.show()\n \
      \       self.timer = QtCore.QTimer()\n        self.timer.timeout.connect(self.update_display)\n\
      \        self.timer.start(50)\n\n    def handle_rx(self, pdu_msg):\n       \
      \ if pmt.is_pair(pdu_msg):\n            self.pdu_queue.append(pdu_msg)\n\n \
      \   def update_display(self):\n        while self.pdu_queue:\n            pdu_msg\
      \ = self.pdu_queue.pop(0)\n            meta = pmt.car(pdu_msg)\n           \
      \ data = pmt.cdr(pdu_msg)\n\n            if meta == pmt.PMT_NIL:\n         \
      \       meta_str = \"None\"\n            else:\n                meta_str = str(meta)\n\
      \n            if data == pmt.PMT_NIL:\n                data_str = \"None\"\n\
      \            elif pmt.is_u8vector(data):\n                data_str = str(list(pmt.u8vector_elements(data)))\n\
      \            elif pmt.is_c32vector(data):\n                data_str = str(list(pmt.c32vector_elements(data)))\n\
      \            else:\n                data_str = str(data)\n\n            self.display.append(f\"\
      Meta: {meta_str}\\nData: {data_str}\\n{'-'*60}\")"
    affinity: ''
    alias: ''
    comment: ''
    maxoutbuf: '0'
    minoutbuf: '0'
  states:
    _io_cache: ('PDU-u8 Probe', 'pdu_probe_qt', [], [('in', 'message', 1)], [], '\n    PDU
      unit8 Probe.\n    ', [])
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [256, 136.0]
    rotation: 0
    state: disabled
- name: epy_block_2_0
  id: epy_block
  parameters:
    _source_code: "from gnuradio import gr\nimport pmt\nimport threading\nfrom collections\
      \ import deque\nimport time\n\nclass msgQueue(gr.basic_block):\n    \"\"\"\n\
      \    Message Queue Block with Priority Handling (PDU-based)\n    ------------------------------------------------------\n\
      \    Maintains 4 queues:\n        TX: normal, emergency\n        RX: normal,\
      \ emergency\n    Always prioritizes emergency messages.\n\n    PDU format:\n\
      \        meta: empty\n        data (pmt.u8vector):\n            [emergency(1B),\
      \ src_addr(1B), dst_addr(1B), message payload bytes]\n    \"\"\"\n\n    def\
      \ __init__(self):\n        gr.basic_block.__init__(self, name=\"MsgQueue\",\
      \ in_sig=[], out_sig=[])\n\n        # Message ports\n        self.message_port_register_in(pmt.intern(\"\
      in_tx\"))\n        self.set_msg_handler(pmt.intern(\"in_tx\"), self.handle_in_tx)\n\
      \        self.message_port_register_in(pmt.intern(\"in_rx\"))\n        self.set_msg_handler(pmt.intern(\"\
      in_rx\"), self.handle_in_rx)\n\n        self.message_port_register_out(pmt.intern(\"\
      out_tx\"))\n        self.message_port_register_out(pmt.intern(\"out_rx\"))\n\
      \n        # Queues\n        self.tx_emergency = deque()\n        self.tx_normal\
      \ = deque()\n        self.rx_emergency = deque()\n        self.rx_normal = deque()\n\
      \n        # Thread control\n        self.running = True\n        self.tx_thread\
      \ = threading.Thread(target=self.tx_loop, daemon=True)\n        self.rx_thread\
      \ = threading.Thread(target=self.rx_loop, daemon=True)\n        self.tx_thread.start()\n\
      \        self.rx_thread.start()\n\n    # ---------------- Handlers ----------------\n\
      \    def handle_in_tx(self, msg_pmt):\n        if pmt.is_pair(msg_pmt):\n  \
      \          data_pmt = pmt.cdr(msg_pmt)\n            if pmt.is_u8vector(data_pmt)\
      \ and len(pmt.u8vector_elements(data_pmt)) >= 3:\n                # Determine\
      \ emergency from first byte of data\n                emergency_flag = bool(pmt.u8vector_elements(data_pmt)[0])\n\
      \                if emergency_flag:\n                    self.tx_emergency.append(msg_pmt)\n\
      \                else:\n                    self.tx_normal.append(msg_pmt)\n\
      \n    def handle_in_rx(self, msg_pmt):\n        if pmt.is_pair(msg_pmt):\n \
      \           data_pmt = pmt.cdr(msg_pmt)\n            if pmt.is_u8vector(data_pmt)\
      \ and len(pmt.u8vector_elements(data_pmt)) >= 3:\n                emergency_flag\
      \ = bool(pmt.u8vector_elements(data_pmt)[0])\n                if emergency_flag:\n\
      \                    self.rx_emergency.append(msg_pmt)\n                else:\n\
      \                    self.rx_normal.append(msg_pmt)\n\n    # ----------------\
      \ TX Loop ----------------\n    def tx_loop(self):\n        while self.running:\n\
      \            msg = None\n            if self.tx_emergency:\n               \
      \ msg = self.tx_emergency.popleft()\n            elif self.tx_normal:\n    \
      \            msg = self.tx_normal.popleft()\n\n            if msg is not None:\n\
      \                # Pass PDU unchanged (meta empty, data includes headers in\
      \ first 3 bytes)\n                out_pdu = pmt.cons(pmt.make_dict(), pmt.cdr(msg))\n\
      \                self.message_port_pub(pmt.intern(\"out_tx\"), out_pdu)\n  \
      \          else:\n                time.sleep(0.01)\n\n    # ----------------\
      \ RX Loop ----------------\n    def rx_loop(self):\n        while self.running:\n\
      \            msg = None\n            if self.rx_emergency:\n               \
      \ msg = self.rx_emergency.popleft()\n            elif self.rx_normal:\n    \
      \            msg = self.rx_normal.popleft()\n\n            if msg is not None:\n\
      \                out_pdu = pmt.cons(pmt.make_dict(), pmt.cdr(msg))\n       \
      \         self.message_port_pub(pmt.intern(\"out_rx\"), out_pdu)\n         \
      \   else:\n                time.sleep(0.01)\n\n    # ---------------- Stop ----------------\n\
      \    def stop(self):\n        self.running = False\n        self.tx_thread.join()\n\
      \        self.rx_thread.join()\n        return True"
    affinity: ''
    alias: ''
    comment: ''
    maxoutbuf: '0'
    minoutbuf: '0'
  states:
    _io_cache: '(''MsgQueue'', ''msgQueue'', [], [(''in_rx'', ''message'', 1), (''in_tx'',
      ''message'', 1)], [(''out_rx'', ''message'', 1), (''out_tx'', ''message'', 1)],
      ''\n    Message Queue Block with Priority Handling (PDU-based)\n    ------------------------------------------------------\n    Maintains
      4 queues:\n        TX: normal, emergency\n        RX: normal, emergency\n    Always
      prioritizes emergency messages.\n\n    PDU format:\n        meta: empty\n        data
      (pmt.u8vector):\n            [emergency(1B), src_addr(1B), dst_addr(1B), message
      payload bytes]\n    '', [])'
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2424, 256.0]
    rotation: 0
    state: enabled
- name: epy_block_3_1
  id: epy_block
  parameters:
    _source_code: "from gnuradio import gr\nfrom PyQt5 import QtWidgets, QtCore\n\
      import sys\nimport numpy as np\n\nclass simple_uint8_probe(gr.basic_block):\n\
      \    \"\"\"\n    UInt8 Stream Probe.\n    \"\"\"\n    def __init__(self):\n\
      \        gr.basic_block.__init__(self,\n            name=\"Tsu8 Probe\",\n \
      \           in_sig=[np.uint8],\n            out_sig=[]\n        )\n\n      \
      \  self.queue = []\n        self.total_bytes = 0\n        self.total_bits =\
      \ 0\n\n\n        self.app = QtWidgets.QApplication.instance() or QtWidgets.QApplication(sys.argv)\n\
      \        self.window = QtWidgets.QWidget()\n        self.window.setWindowTitle(\"\
      UInt8 Probe\")\n        self.window.resize(400, 400)\n        layout = QtWidgets.QVBoxLayout(self.window)\n\
      \        self.display = QtWidgets.QTextEdit()\n        self.display.setReadOnly(True)\n\
      \        layout.addWidget(self.display)\n        self.window.show()\n      \
      \  self.timer = QtCore.QTimer()\n        self.timer.timeout.connect(self.update_display)\n\
      \        self.timer.start(50)\n\n    def general_work(self, input_items, output_items):\n\
      \        in0 = input_items[0]\n        for val in in0:\n            self.queue.append(val)\n\
      \            self.total_bytes += 1\n            self.total_bits += 8\n     \
      \   self.consume(0, len(in0))\n        return 0\n\n    def update_display(self):\n\
      \        while self.queue:\n            val = self.queue.pop(0)\n          \
      \  self.display.append(f\"Value: {val} | Total Bytes: {self.total_bytes} | Total\
      \ Bits: {self.total_bits}\")"
    affinity: ''
    alias: ''
    comment: ''
    maxoutbuf: '0'
    minoutbuf: '0'
  states:
    _io_cache: ('Tsu8 Probe', 'simple_uint8_probe', [], [('0', 'byte', 1)], [], '\n    UInt8
      Stream Probe.\n    ', [])
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [272, 176.0]
    rotation: 0
    state: disabled
- name: epy_block_4
  id: epy_block
  parameters:
    _source_code: "from gnuradio import gr\nimport pmt\nimport threading\nfrom collections\
      \ import deque\nimport time\n\nclass ARQ(gr.basic_block):\n    \"\"\"\n    ARQ\
      \ Transport Layer.\n    Stop-and-Wait ARQ with msg_id, ACKs, and emergency prioritization.\n\
      \n    Upper layer PDU:\n        meta: empty\n        data: [emergency(1B), src_addr(1B),\
      \ dst_addr(1B), payload bytes]\n\n    Lower layer PDU:\n        meta: empty\n\
      \        data: [msg_id(1B), ack(1B), emergency(1B), src_addr(1B), dst_addr(1B),\
      \ payload bytes (fixed 1024B)]\n    \"\"\"\n    def __init__(self, retransmit_delay=0.2,\
      \ max_retries=5):\n        gr.basic_block.__init__(self, name=\"Framing & ARQ\"\
      , in_sig=[], out_sig=[])\n\n        self.payload_len = 1024\n        self.retransmit_delay\
      \ = retransmit_delay\n        self.max_retries = max_retries\n\n        self.message_port_register_in(pmt.intern(\"\
      in_tx\"))\n        self.set_msg_handler(pmt.intern(\"in_tx\"), self.handle_in_tx)\n\
      \        self.message_port_register_in(pmt.intern(\"in_rx\"))\n        self.set_msg_handler(pmt.intern(\"\
      in_rx\"), self.handle_in_rx)\n        self.message_port_register_out(pmt.intern(\"\
      out_tx\"))\n        self.message_port_register_out(pmt.intern(\"out_rx\"))\n\
      \n        self.tx_emergency = deque()\n        self.tx_normal = deque()\n  \
      \      self.rx_queue = deque()\n\n        self.pending_ack = {} \n        self.next_msg_id\
      \ = 0\n\n        self.last_delivered = {}\n\n        self.running = True\n \
      \       self.tx_thread = threading.Thread(target=self.tx_loop, daemon=True)\n\
      \        self.rx_thread = threading.Thread(target=self.rx_loop, daemon=True)\n\
      \        self.tx_thread.start()\n        self.rx_thread.start()\n\n    def handle_in_tx(self,\
      \ pdu):\n        if pmt.is_pair(pdu):\n            data = list(pmt.u8vector_elements(pmt.cdr(pdu)))\n\
      \            emergency = bool(data[0])\n            if emergency:\n        \
      \        self.tx_emergency.append(pdu)\n            else:\n                self.tx_normal.append(pdu)\n\
      \n    def handle_in_rx(self, pdu):\n        if pmt.is_pair(pdu):\n         \
      \   self.rx_queue.append(pdu)\n\n    def tx_loop(self):\n        while self.running:\n\
      \            pdu = None\n            if self.tx_emergency:\n               \
      \ pdu = self.tx_emergency.popleft()\n            elif self.tx_normal:\n    \
      \            pdu = self.tx_normal.popleft()\n\n            if pdu:\n       \
      \         data = list(pmt.u8vector_elements(pmt.cdr(pdu)))\n               \
      \ emergency, src, dst = data[:3]\n                payload = data[3:]\n\n   \
      \             if len(payload) < self.payload_len:\n                    payload\
      \ += [0]*(self.payload_len - len(payload))\n                elif len(payload)\
      \ > self.payload_len:\n                    payload = payload[:self.payload_len]\n\
      \n                msg_id = self.next_msg_id\n                self.next_msg_id\
      \ = (self.next_msg_id + 1) % 256\n\n                lower_data = [msg_id, 0,\
      \ emergency, src, dst] + payload\n                lower_pdu = pmt.cons(pmt.make_dict(),\
      \ pmt.init_u8vector(len(lower_data), lower_data))\n\n                self.pending_ack[msg_id]\
      \ = (lower_pdu, time.time(), 1)\n                self.message_port_pub(pmt.intern(\"\
      out_tx\"), lower_pdu)\n\n            now = time.time()\n            to_remove\
      \ = []\n            for mid, (frame, ts, retries) in self.pending_ack.items():\n\
      \                if now - ts > self.retransmit_delay:\n                    if\
      \ retries >= self.max_retries:\n                        to_remove.append(mid)\n\
      \                    else:\n                        self.pending_ack[mid] =\
      \ (frame, now, retries+1)\n                        self.message_port_pub(pmt.intern(\"\
      out_tx\"), frame)\n            for mid in to_remove:\n                del self.pending_ack[mid]\n\
      \n            time.sleep(0.01)\n\n    def rx_loop(self):\n        while self.running:\n\
      \            if self.rx_queue:\n                pdu = self.rx_queue.popleft()\n\
      \                data = list(pmt.u8vector_elements(pmt.cdr(pdu)))\n        \
      \        if len(data) < 5:\n                    continue\n                msg_id,\
      \ ack_flag, emergency, src, dst = data[:5]\n                payload = data[5:]\n\
      \n                if ack_flag:\n                    if msg_id in self.pending_ack:\n\
      \                        del self.pending_ack[msg_id]\n                    continue\n\
      \n                ack_data = [msg_id, 1, emergency, src, dst] + [0]*self.payload_len\n\
      \                ack_pdu = pmt.cons(pmt.make_dict(), pmt.init_u8vector(len(ack_data),\
      \ ack_data))\n                self.message_port_pub(pmt.intern(\"out_tx\"),\
      \ ack_pdu)\n\n                key = (src, dst)\n                if self.last_delivered.get(key)\
      \ != msg_id:\n                    self.last_delivered[key] = msg_id\n      \
      \              payload_stripped = [b for b in payload if b != 0]\n         \
      \           upper_data = [emergency, src, dst] + payload_stripped\n        \
      \            upper_pdu = pmt.cons(pmt.make_dict(), pmt.init_u8vector(len(upper_data),\
      \ upper_data))\n                    self.message_port_pub(pmt.intern(\"out_rx\"\
      ), upper_pdu)\n            else:\n                time.sleep(0.01)\n\n    def\
      \ stop(self):\n        self.running = False\n        self.tx_thread.join()\n\
      \        self.rx_thread.join()\n        return True"
    affinity: ''
    alias: ''
    comment: ''
    max_retries: '50'
    maxoutbuf: '0'
    minoutbuf: '0'
    retransmit_delay: '0.2'
  states:
    _io_cache: '(''Framing & ARQ'', ''ARQ'', [(''retransmit_delay'', ''0.2''), (''max_retries'',
      ''5'')], [(''in_rx'', ''message'', 1), (''in_tx'', ''message'', 1)], [(''out_rx'',
      ''message'', 1), (''out_tx'', ''message'', 1)], ''\n    ARQ Transport Layer.\n    Stop-and-Wait
      ARQ with msg_id, ACKs, and emergency prioritization.\n\n    Upper layer PDU:\n        meta:
      empty\n        data: [emergency(1B), src_addr(1B), dst_addr(1B), payload bytes]\n\n    Lower
      layer PDU:\n        meta: empty\n        data: [msg_id(1B), ack(1B), emergency(1B),
      src_addr(1B), dst_addr(1B), payload bytes (fixed 1024B)]\n    '', [''max_retries'',
      ''retransmit_delay''])'
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1112, 360.0]
    rotation: 0
    state: disabled
- name: epy_block_4_0
  id: epy_block
  parameters:
    _source_code: "from gnuradio import gr\nimport pmt\nimport threading\nfrom collections\
      \ import deque\nimport time\n\nclass ARQ(gr.basic_block):\n    \"\"\"\n    ARQ\
      \ Transport Layer (PDU-based, fixed-length frames)\n    ----------------------------------------------------\n\
      \    Stop-and-Wait ARQ with msg_id, ACKs, and emergency prioritization.\n\n\
      \    Upper layer PDU:\n        meta: empty\n        data: [emergency(1B), src_addr(1B),\
      \ dst_addr(1B), payload bytes]\n\n    Lower layer PDU:\n        meta: empty\n\
      \        data: [msg_id(1B), ack(1B), emergency(1B), src_addr(1B), dst_addr(1B),\
      \ payload bytes (fixed 1024B)]\n    \"\"\"\n    def __init__(self, retransmit_delay=0.2,\
      \ max_retries=5):\n        gr.basic_block.__init__(self, name=\"ARQ\", in_sig=[],\
      \ out_sig=[])\n\n        self.payload_len = 1024\n        self.retransmit_delay\
      \ = retransmit_delay\n        self.max_retries = max_retries\n\n        # Ports\n\
      \        self.message_port_register_in(pmt.intern(\"in_tx\"))\n        self.set_msg_handler(pmt.intern(\"\
      in_tx\"), self.handle_in_tx)\n        self.message_port_register_in(pmt.intern(\"\
      in_rx\"))\n        self.set_msg_handler(pmt.intern(\"in_rx\"), self.handle_in_rx)\n\
      \        self.message_port_register_out(pmt.intern(\"out_tx\"))\n        self.message_port_register_out(pmt.intern(\"\
      out_rx\"))\n\n        # Queues\n        self.tx_emergency = deque()\n      \
      \  self.tx_normal = deque()\n        self.rx_queue = deque()\n\n        # Pending\
      \ ACKs\n        self.pending_ack = {}  # msg_id -> (frame_pdu, timestamp, retries)\n\
      \        self.next_msg_id = 0\n\n        # Duplicate filter\n        self.last_delivered\
      \ = {}  # (src, dst) -> msg_id\n\n        # Threads\n        self.running =\
      \ True\n        self.tx_thread = threading.Thread(target=self.tx_loop, daemon=True)\n\
      \        self.rx_thread = threading.Thread(target=self.rx_loop, daemon=True)\n\
      \        self.tx_thread.start()\n        self.rx_thread.start()\n\n    # ----------------\
      \ Handlers ----------------\n    def handle_in_tx(self, pdu):\n        if pmt.is_pair(pdu):\n\
      \            data = list(pmt.u8vector_elements(pmt.cdr(pdu)))\n            emergency\
      \ = bool(data[0])\n            if emergency:\n                self.tx_emergency.append(pdu)\n\
      \            else:\n                self.tx_normal.append(pdu)\n\n    def handle_in_rx(self,\
      \ pdu):\n        if pmt.is_pair(pdu):\n            self.rx_queue.append(pdu)\n\
      \n    # ---------------- TX Loop ----------------\n    def tx_loop(self):\n\
      \        while self.running:\n            pdu = None\n            if self.tx_emergency:\n\
      \                pdu = self.tx_emergency.popleft()\n            elif self.tx_normal:\n\
      \                pdu = self.tx_normal.popleft()\n\n            if pdu:\n   \
      \             data = list(pmt.u8vector_elements(pmt.cdr(pdu)))\n           \
      \     emergency, src, dst = data[:3]\n                payload = data[3:]\n\n\
      \                # Pad payload to 1024B\n                if len(payload) < self.payload_len:\n\
      \                    payload += [0]*(self.payload_len - len(payload))\n    \
      \            elif len(payload) > self.payload_len:\n                    payload\
      \ = payload[:self.payload_len]\n\n                msg_id = self.next_msg_id\n\
      \                self.next_msg_id = (self.next_msg_id + 1) % 256\n\n       \
      \         lower_data = [msg_id, 0, emergency, src, dst] + payload\n        \
      \        lower_pdu = pmt.cons(pmt.make_dict(), pmt.init_u8vector(len(lower_data),\
      \ lower_data))\n\n                self.pending_ack[msg_id] = (lower_pdu, time.time(),\
      \ 1)\n                self.message_port_pub(pmt.intern(\"out_tx\"), lower_pdu)\n\
      \n            # Retransmit pending frames\n            now = time.time()\n \
      \           to_remove = []\n            for mid, (frame, ts, retries) in self.pending_ack.items():\n\
      \                if now - ts > self.retransmit_delay:\n                    if\
      \ retries >= self.max_retries:\n                        to_remove.append(mid)\n\
      \                    else:\n                        self.pending_ack[mid] =\
      \ (frame, now, retries+1)\n                        self.message_port_pub(pmt.intern(\"\
      out_tx\"), frame)\n            for mid in to_remove:\n                del self.pending_ack[mid]\n\
      \n            time.sleep(0.01)\n\n    # ---------------- RX Loop ----------------\n\
      \    def rx_loop(self):\n        while self.running:\n            if self.rx_queue:\n\
      \                pdu = self.rx_queue.popleft()\n                data = list(pmt.u8vector_elements(pmt.cdr(pdu)))\n\
      \                if len(data) < 5:\n                    continue\n         \
      \       msg_id, ack_flag, emergency, src, dst = data[:5]\n                payload\
      \ = data[5:]\n\n                if ack_flag:\n                    if msg_id\
      \ in self.pending_ack:\n                        del self.pending_ack[msg_id]\n\
      \                    continue\n\n                # Send ACK\n              \
      \  ack_data = [msg_id, 1, emergency, src, dst] + [0]*self.payload_len\n    \
      \            ack_pdu = pmt.cons(pmt.make_dict(), pmt.init_u8vector(len(ack_data),\
      \ ack_data))\n                self.message_port_pub(pmt.intern(\"out_tx\"),\
      \ ack_pdu)\n\n                # Deliver only if not duplicate\n            \
      \    key = (src, dst)\n                if self.last_delivered.get(key) != msg_id:\n\
      \                    self.last_delivered[key] = msg_id\n                   \
      \ payload_stripped = [b for b in payload if b != 0]\n                    upper_data\
      \ = [emergency, src, dst] + payload_stripped\n                    upper_pdu\
      \ = pmt.cons(pmt.make_dict(), pmt.init_u8vector(len(upper_data), upper_data))\n\
      \                    self.message_port_pub(pmt.intern(\"out_rx\"), upper_pdu)\n\
      \            else:\n                time.sleep(0.01)\n\n    # ----------------\
      \ Stop ----------------\n    def stop(self):\n        self.running = False\n\
      \        self.tx_thread.join()\n        self.rx_thread.join()\n        return\
      \ True"
    affinity: ''
    alias: ''
    comment: ''
    max_retries: '50'
    maxoutbuf: '0'
    minoutbuf: '0'
    retransmit_delay: '0.2'
  states:
    _io_cache: '(''ARQ'', ''ARQ'', [(''retransmit_delay'', ''0.2''), (''max_retries'',
      ''5'')], [(''in_rx'', ''message'', 1), (''in_tx'', ''message'', 1)], [(''out_rx'',
      ''message'', 1), (''out_tx'', ''message'', 1)], ''\n    ARQ Transport Layer
      (PDU-based, fixed-length frames)\n    ----------------------------------------------------\n    Stop-and-Wait
      ARQ with msg_id, ACKs, and emergency prioritization.\n\n    Upper layer PDU:\n        meta:
      empty\n        data: [emergency(1B), src_addr(1B), dst_addr(1B), payload bytes]\n\n    Lower
      layer PDU:\n        meta: empty\n        data: [msg_id(1B), ack(1B), emergency(1B),
      src_addr(1B), dst_addr(1B), payload bytes (fixed 1024B)]\n    '', [''max_retries'',
      ''retransmit_delay''])'
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2392, 368.0]
    rotation: 0
    state: enabled
- name: epy_block_7
  id: epy_block
  parameters:
    _source_code: "from gnuradio import gr\nimport pmt\nfrom collections import deque\n\
      import threading\nimport time\n\nclass address_filter(gr.basic_block):\n   \
      \ \"\"\"\n    Address Filtering.\n    - TX: Pass-through from upper layer to\
      \ lower layer.\n    - RX: Forward frames to upper layer only if dst_addr matches\
      \ my_addr.\n    - Frame format (PDU):\n        meta: empty\n        data: [msg_id,\
      \ ack, emergency, src_addr, dst_addr, payload bytes (1024B)]\n    \"\"\"\n\n\
      \    def __init__(self, my_addr=0):\n        gr.basic_block.__init__(self, name=\"\
      AddressFilter\", in_sig=[], out_sig=[])\n\n        self.my_addr = my_addr\n\n\
      \        self.message_port_register_in(pmt.intern(\"in_tx\"))\n        self.set_msg_handler(pmt.intern(\"\
      in_tx\"), self.handle_in_tx)\n\n        self.message_port_register_in(pmt.intern(\"\
      in_rx\"))\n        self.set_msg_handler(pmt.intern(\"in_rx\"), self.handle_in_rx)\n\
      \n        self.message_port_register_out(pmt.intern(\"out_tx\"))\n        self.message_port_register_out(pmt.intern(\"\
      out_rx\"))\n\n        self.tx_queue = deque()\n        self.rx_queue = deque()\n\
      \n        self.running = True\n        self.tx_thread = threading.Thread(target=self.tx_loop,\
      \ daemon=True)\n        self.rx_thread = threading.Thread(target=self.rx_loop,\
      \ daemon=True)\n        self.tx_thread.start()\n        self.rx_thread.start()\n\
      \n\n    def handle_in_tx(self, pdu):\n        if pmt.is_pair(pdu):\n       \
      \     self.tx_queue.append(pdu)\n\n    def handle_in_rx(self, pdu):\n      \
      \  if pmt.is_pair(pdu):\n            self.rx_queue.append(pdu)\n\n    def tx_loop(self):\n\
      \        while self.running:\n            if self.tx_queue:\n              \
      \  pdu = self.tx_queue.popleft()\n                self.message_port_pub(pmt.intern(\"\
      out_tx\"), pdu)\n            else:\n                time.sleep(0.01)\n\n   \
      \ def rx_loop(self):\n        while self.running:\n            if self.rx_queue:\n\
      \                pdu = self.rx_queue.popleft()\n                data = list(pmt.u8vector_elements(pmt.cdr(pdu)))\n\
      \                if len(data) < 5:\n                    continue\n         \
      \       dst_addr = data[4]\n                if dst_addr == self.my_addr:\n \
      \                   self.message_port_pub(pmt.intern(\"out_rx\"), pdu)\n   \
      \         else:\n                time.sleep(0.01)\n\n    def stop(self):\n \
      \       self.running = False\n        self.tx_thread.join()\n        self.rx_thread.join()\n\
      \        return True"
    affinity: ''
    alias: ''
    comment: ''
    maxoutbuf: '0'
    minoutbuf: '0'
    my_addr: '0x00'
  states:
    _io_cache: '(''AddressFilter'', ''address_filter'', [(''my_addr'', ''0'')], [(''in_rx'',
      ''message'', 1), (''in_tx'', ''message'', 1)], [(''out_rx'', ''message'', 1),
      (''out_tx'', ''message'', 1)], ''\n    Address Filtering.\n    - TX: Pass-through
      from upper layer to lower layer.\n    - RX: Forward frames to upper layer only
      if dst_addr matches my_addr.\n    - Frame format (PDU):\n        meta: empty\n        data:
      [msg_id, ack, emergency, src_addr, dst_addr, payload bytes (1024B)]\n    '',
      [''my_addr''])'
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1136, 432.0]
    rotation: 0
    state: disabled
- name: epy_block_7_0
  id: epy_block
  parameters:
    _source_code: "from gnuradio import gr\nimport pmt\nfrom collections import deque\n\
      import threading\nimport time\n\nclass address_filter_block(gr.basic_block):\n\
      \    \"\"\"\n    Address Filtering Block (PDU-based)\n    -----------------------------------\n\
      \    - TX: Pass-through from upper layer to lower layer.\n    - RX: Forward\
      \ frames to upper layer only if dst_addr matches my_addr.\n    - Frame format\
      \ (PDU):\n        meta: empty\n        data: [msg_id, ack, emergency, src_addr,\
      \ dst_addr, payload bytes (1024B)]\n    \"\"\"\n\n    def __init__(self, my_addr=0):\n\
      \        gr.basic_block.__init__(self, name=\"AddressFilter\", in_sig=[], out_sig=[])\n\
      \n        self.my_addr = my_addr\n\n        # Message ports\n        self.message_port_register_in(pmt.intern(\"\
      in_tx\"))  # upper -> lower\n        self.set_msg_handler(pmt.intern(\"in_tx\"\
      ), self.handle_in_tx)\n\n        self.message_port_register_in(pmt.intern(\"\
      in_rx\"))  # lower -> upper\n        self.set_msg_handler(pmt.intern(\"in_rx\"\
      ), self.handle_in_rx)\n\n        self.message_port_register_out(pmt.intern(\"\
      out_tx\"))\n        self.message_port_register_out(pmt.intern(\"out_rx\"))\n\
      \n        # Queues\n        self.tx_queue = deque()\n        self.rx_queue =\
      \ deque()\n\n        # Threads\n        self.running = True\n        self.tx_thread\
      \ = threading.Thread(target=self.tx_loop, daemon=True)\n        self.rx_thread\
      \ = threading.Thread(target=self.rx_loop, daemon=True)\n        self.tx_thread.start()\n\
      \        self.rx_thread.start()\n\n    # ---------------- Handlers ----------------\n\
      \    def handle_in_tx(self, pdu):\n        if pmt.is_pair(pdu):  # Expecting\
      \ PDU (meta, data)\n            self.tx_queue.append(pdu)\n\n    def handle_in_rx(self,\
      \ pdu):\n        if pmt.is_pair(pdu):  # Expecting PDU (meta, data)\n      \
      \      self.rx_queue.append(pdu)\n\n    # ---------------- TX loop ----------------\n\
      \    def tx_loop(self):\n        while self.running:\n            if self.tx_queue:\n\
      \                pdu = self.tx_queue.popleft()\n                # Pass-through\
      \ to lower layer\n                self.message_port_pub(pmt.intern(\"out_tx\"\
      ), pdu)\n            else:\n                time.sleep(0.01)\n\n    # ----------------\
      \ RX loop ----------------\n    def rx_loop(self):\n        while self.running:\n\
      \            if self.rx_queue:\n                pdu = self.rx_queue.popleft()\n\
      \                data = list(pmt.u8vector_elements(pmt.cdr(pdu)))\n        \
      \        if len(data) < 5:\n                    continue  # invalid frame\n\
      \                dst_addr = data[4]  # dst_addr is 5th byte\n              \
      \  if dst_addr == self.my_addr:\n                    # Forward to upper layer\
      \ (pass PDU as-is)\n                    self.message_port_pub(pmt.intern(\"\
      out_rx\"), pdu)\n                # else: silently drop\n            else:\n\
      \                time.sleep(0.01)\n\n    # ---------------- Stop ----------------\n\
      \    def stop(self):\n        self.running = False\n        self.tx_thread.join()\n\
      \        self.rx_thread.join()\n        return True"
    affinity: ''
    alias: ''
    comment: ''
    maxoutbuf: '0'
    minoutbuf: '0'
    my_addr: '0x01'
  states:
    _io_cache: '(''AddressFilter'', ''address_filter_block'', [(''my_addr'', ''0'')],
      [(''in_rx'', ''message'', 1), (''in_tx'', ''message'', 1)], [(''out_rx'', ''message'',
      1), (''out_tx'', ''message'', 1)], ''\n    Address Filtering Block (PDU-based)\n    -----------------------------------\n    -
      TX: Pass-through from upper layer to lower layer.\n    - RX: Forward frames
      to upper layer only if dst_addr matches my_addr.\n    - Frame format (PDU):\n        meta:
      empty\n        data: [msg_id, ack, emergency, src_addr, dst_addr, payload bytes
      (1024B)]\n    '', [''my_addr''])'
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2416, 440.0]
    rotation: 0
    state: enabled
- name: epy_block_8
  id: epy_block
  parameters:
    _source_code: "from gnuradio import gr\nimport pmt\nfrom collections import deque\n\
      import threading\nimport time\nimport zlib\n\nclass crc(gr.basic_block):\n \
      \   \"\"\"\n    CRC Block.\n    TX: Prepend CRC32 to data payload and forward\
      \ PDU.\n    RX: Verify CRC32 at start of data; strip CRC and forward if valid.\n\
      \    \"\"\"\n\n    def __init__(self):\n        gr.basic_block.__init__(self,\
      \ name=\"CRC\", in_sig=[], out_sig=[])\n\n        self.message_port_register_in(pmt.intern(\"\
      in_tx\"))\n        self.set_msg_handler(pmt.intern(\"in_tx\"), self.handle_in_tx)\n\
      \        self.message_port_register_in(pmt.intern(\"in_rx\"))\n        self.set_msg_handler(pmt.intern(\"\
      in_rx\"), self.handle_in_rx)\n\n        self.message_port_register_out(pmt.intern(\"\
      out_tx\"))\n        self.message_port_register_out(pmt.intern(\"out_rx\"))\n\
      \n        self.tx_queue = deque()\n        self.rx_queue = deque()\n\n     \
      \   self.running = True\n        self.tx_thread = threading.Thread(target=self.tx_loop,\
      \ daemon=True)\n        self.rx_thread = threading.Thread(target=self.rx_loop,\
      \ daemon=True)\n        self.tx_thread.start()\n        self.rx_thread.start()\n\
      \n    def handle_in_tx(self, pdu):\n        if pmt.is_pair(pdu):\n         \
      \   self.tx_queue.append(pdu)\n\n    def handle_in_rx(self, pdu):\n        if\
      \ pmt.is_pair(pdu):\n            self.rx_queue.append(pdu)\n\n    def tx_loop(self):\n\
      \        while self.running:\n            if self.tx_queue:\n              \
      \  pdu = self.tx_queue.popleft()\n                meta = pmt.car(pdu)\n    \
      \            data = list(pmt.u8vector_elements(pmt.cdr(pdu)))\n\n          \
      \      crc_value = zlib.crc32(bytes(data)) & 0xFFFFFFFF\n                crc_bytes\
      \ = [(crc_value >> 24) & 0xFF,\n                             (crc_value >> 16)\
      \ & 0xFF,\n                             (crc_value >> 8) & 0xFF,\n         \
      \                    crc_value & 0xFF]\n\n                data_with_crc = crc_bytes\
      \ + data\n                data_pmt = pmt.init_u8vector(len(data_with_crc), data_with_crc)\n\
      \n                self.message_port_pub(pmt.intern(\"out_tx\"), pmt.cons(pmt.make_dict(),\
      \ data_pmt))\n            else:\n                time.sleep(0.01)\n\n    def\
      \ rx_loop(self):\n        while self.running:\n            if self.rx_queue:\n\
      \                pdu = self.rx_queue.popleft()\n                meta = pmt.car(pdu)\n\
      \                data = list(pmt.u8vector_elements(pmt.cdr(pdu)))\n        \
      \        if len(data) < 4:\n                    continue\n\n               \
      \ recv_crc_bytes = data[:4]\n                payload = data[4:]\n          \
      \      recv_crc = (recv_crc_bytes[0] << 24) | (recv_crc_bytes[1] << 16) | \\\
      \n                           (recv_crc_bytes[2] << 8) | recv_crc_bytes[3]\n\
      \                computed_crc = zlib.crc32(bytes(payload)) & 0xFFFFFFFF\n  \
      \              if recv_crc == computed_crc:\n                    payload_pmt\
      \ = pmt.init_u8vector(len(payload), payload)\n                    self.message_port_pub(pmt.intern(\"\
      out_rx\"), pmt.cons(pmt.make_dict(), payload_pmt))\n            else:\n    \
      \            time.sleep(0.01)\n\n    def stop(self):\n        self.running =\
      \ False\n        self.tx_thread.join()\n        self.rx_thread.join()\n    \
      \    return True"
    affinity: ''
    alias: ''
    comment: ''
    maxoutbuf: '0'
    minoutbuf: '0'
  states:
    _io_cache: '(''CRC'', ''crc'', [], [(''in_rx'', ''message'', 1), (''in_tx'', ''message'',
      1)], [(''out_rx'', ''message'', 1), (''out_tx'', ''message'', 1)], ''\n    CRC
      Block.\n    TX: Prepend CRC32 to data payload and forward PDU.\n    RX: Verify
      CRC32 at start of data; strip CRC and forward if valid.\n    '', [])'
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1144, 504.0]
    rotation: 0
    state: disabled
- name: epy_block_8_0
  id: epy_block
  parameters:
    _source_code: "from gnuradio import gr\nimport pmt\nfrom collections import deque\n\
      import threading\nimport time\nimport zlib\n\nclass crc_block(gr.basic_block):\n\
      \    \"\"\"\n    CRC Block (PDU-based)\n    ---------------------\n    TX: Prepend\
      \ CRC32 to data payload and forward PDU.\n    RX: Verify CRC32 at start of data;\
      \ strip CRC and forward if valid.\n    \"\"\"\n\n    def __init__(self):\n \
      \       gr.basic_block.__init__(self, name=\"CRC_PDU\", in_sig=[], out_sig=[])\n\
      \n        # Ports\n        self.message_port_register_in(pmt.intern(\"in_tx\"\
      ))\n        self.set_msg_handler(pmt.intern(\"in_tx\"), self.handle_in_tx)\n\
      \        self.message_port_register_in(pmt.intern(\"in_rx\"))\n        self.set_msg_handler(pmt.intern(\"\
      in_rx\"), self.handle_in_rx)\n\n        self.message_port_register_out(pmt.intern(\"\
      out_tx\"))\n        self.message_port_register_out(pmt.intern(\"out_rx\"))\n\
      \n        # Queues\n        self.tx_queue = deque()\n        self.rx_queue =\
      \ deque()\n\n        # Threads\n        self.running = True\n        self.tx_thread\
      \ = threading.Thread(target=self.tx_loop, daemon=True)\n        self.rx_thread\
      \ = threading.Thread(target=self.rx_loop, daemon=True)\n        self.tx_thread.start()\n\
      \        self.rx_thread.start()\n\n    # ---------------- Handlers ----------------\n\
      \    def handle_in_tx(self, pdu):\n        if pmt.is_pair(pdu):\n          \
      \  self.tx_queue.append(pdu)\n\n    def handle_in_rx(self, pdu):\n        if\
      \ pmt.is_pair(pdu):\n            self.rx_queue.append(pdu)\n\n    # ----------------\
      \ TX loop ----------------\n    def tx_loop(self):\n        while self.running:\n\
      \            if self.tx_queue:\n                pdu = self.tx_queue.popleft()\n\
      \                meta = pmt.car(pdu)  # empty\n                data = list(pmt.u8vector_elements(pmt.cdr(pdu)))\n\
      \n                # Compute CRC32 over the entire payload\n                crc_value\
      \ = zlib.crc32(bytes(data)) & 0xFFFFFFFF\n                crc_bytes = [(crc_value\
      \ >> 24) & 0xFF,\n                             (crc_value >> 16) & 0xFF,\n \
      \                            (crc_value >> 8) & 0xFF,\n                    \
      \         crc_value & 0xFF]\n\n                # Prepend CRC to data\n     \
      \           data_with_crc = crc_bytes + data\n                data_pmt = pmt.init_u8vector(len(data_with_crc),\
      \ data_with_crc)\n\n                # Forward PDU (meta empty, data with CRC)\n\
      \                self.message_port_pub(pmt.intern(\"out_tx\"), pmt.cons(pmt.make_dict(),\
      \ data_pmt))\n            else:\n                time.sleep(0.01)\n\n    # ----------------\
      \ RX loop ----------------\n    def rx_loop(self):\n        while self.running:\n\
      \            if self.rx_queue:\n                pdu = self.rx_queue.popleft()\n\
      \                meta = pmt.car(pdu)  # empty\n                data = list(pmt.u8vector_elements(pmt.cdr(pdu)))\n\
      \                if len(data) < 4:\n                    continue  # invalid\
      \ frame\n\n                # Extract CRC and payload\n                recv_crc_bytes\
      \ = data[:4]\n                payload = data[4:]\n                recv_crc =\
      \ (recv_crc_bytes[0] << 24) | (recv_crc_bytes[1] << 16) | \\\n             \
      \              (recv_crc_bytes[2] << 8) | recv_crc_bytes[3]\n\n            \
      \    # Verify CRC\n                computed_crc = zlib.crc32(bytes(payload))\
      \ & 0xFFFFFFFF\n                if recv_crc == computed_crc:\n             \
      \       # CRC OK: forward PDU without CRC\n                    payload_pmt =\
      \ pmt.init_u8vector(len(payload), payload)\n                    self.message_port_pub(pmt.intern(\"\
      out_rx\"), pmt.cons(pmt.make_dict(), payload_pmt))\n                # else:\
      \ silently drop\n            else:\n                time.sleep(0.01)\n\n   \
      \ # ---------------- Stop ----------------\n    def stop(self):\n        self.running\
      \ = False\n        self.tx_thread.join()\n        self.rx_thread.join()\n  \
      \      return True"
    affinity: ''
    alias: ''
    comment: ''
    maxoutbuf: '0'
    minoutbuf: '0'
  states:
    _io_cache: '(''CRC_PDU'', ''crc_block'', [], [(''in_rx'', ''message'', 1), (''in_tx'',
      ''message'', 1)], [(''out_rx'', ''message'', 1), (''out_tx'', ''message'', 1)],
      ''\n    CRC Block (PDU-based)\n    ---------------------\n    TX: Prepend CRC32
      to data payload and forward PDU.\n    RX: Verify CRC32 at start of data; strip
      CRC and forward if valid.\n    '', [])'
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2424, 512.0]
    rotation: 0
    state: enabled
- name: pdu_pdu_to_tagged_stream_0_0
  id: pdu_pdu_to_tagged_stream
  parameters:
    affinity: ''
    alias: ''
    comment: Header
    maxoutbuf: '0'
    minoutbuf: '0'
    tag: packet_len
    type: byte
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1416.0, 680]
    rotation: 90
    state: disabled
- name: pdu_pdu_to_tagged_stream_0_0_0
  id: pdu_pdu_to_tagged_stream
  parameters:
    affinity: ''
    alias: ''
    comment: Payload
    maxoutbuf: '0'
    minoutbuf: '0'
    tag: packet_len
    type: byte
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1368.0, 680]
    rotation: 90
    state: disabled
- name: pdu_pdu_to_tagged_stream_0_0_0_0
  id: pdu_pdu_to_tagged_stream
  parameters:
    affinity: ''
    alias: ''
    comment: Payload
    maxoutbuf: '0'
    minoutbuf: '0'
    tag: packet_len
    type: byte
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2648.0, 688]
    rotation: 90
    state: enabled
- name: pdu_pdu_to_tagged_stream_0_0_1
  id: pdu_pdu_to_tagged_stream
  parameters:
    affinity: ''
    alias: ''
    comment: Header
    maxoutbuf: '0'
    minoutbuf: '0'
    tag: packet_len
    type: byte
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2696.0, 688]
    rotation: 90
    state: enabled
- name: pdu_tagged_stream_to_pdu_0
  id: pdu_tagged_stream_to_pdu
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    maxoutbuf: '0'
    minoutbuf: '0'
    tag: packet_len
    type: byte
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1120.0, 680]
    rotation: 90
    state: disabled
- name: pdu_tagged_stream_to_pdu_0_0
  id: pdu_tagged_stream_to_pdu
  parameters:
    affinity: ''
    alias: ''
    comment: ''
    maxoutbuf: '0'
    minoutbuf: '0'
    tag: packet_len
    type: byte
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2408.0, 688]
    rotation: 90
    state: disabled
- name: qtgui_const_sink_x_0
  id: qtgui_const_sink_x
  parameters:
    affinity: ''
    alias: ''
    alpha1: '1.0'
    alpha10: '1.0'
    alpha2: '1.0'
    alpha3: '1.0'
    alpha4: '1.0'
    alpha5: '1.0'
    alpha6: '1.0'
    alpha7: '1.0'
    alpha8: '1.0'
    alpha9: '1.0'
    autoscale: 'False'
    axislabels: 'True'
    color1: '"blue"'
    color10: '"dark blue"'
    color2: '"red"'
    color3: '"green"'
    color4: '"black"'
    color5: '"cyan"'
    color6: '"magenta"'
    color7: '"yellow"'
    color8: '"dark red"'
    color9: '"dark green"'
    comment: ''
    grid: 'False'
    gui_hint: ''
    label1: ''
    label10: ''
    label2: ''
    label3: ''
    label4: ''
    label5: ''
    label6: ''
    label7: ''
    label8: ''
    label9: ''
    legend: 'True'
    marker1: '0'
    marker10: '0'
    marker2: '0'
    marker3: '0'
    marker4: '0'
    marker5: '0'
    marker6: '0'
    marker7: '0'
    marker8: '0'
    marker9: '0'
    name: '""'
    nconnections: '1'
    size: '1024'
    style1: '0'
    style10: '0'
    style2: '0'
    style3: '0'
    style4: '0'
    style5: '0'
    style6: '0'
    style7: '0'
    style8: '0'
    style9: '0'
    tr_chan: '0'
    tr_level: '0.0'
    tr_mode: qtgui.TRIG_MODE_FREE
    tr_slope: qtgui.TRIG_SLOPE_POS
    tr_tag: '""'
    type: complex
    update_time: '0.10'
    width1: '1'
    width10: '1'
    width2: '1'
    width3: '1'
    width4: '1'
    width5: '1'
    width6: '1'
    width7: '1'
    width8: '1'
    width9: '1'
    xmax: '2'
    xmin: '-2'
    ymax: '2'
    ymin: '-2'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1552, 1376.0]
    rotation: 0
    state: disabled
- name: qtgui_const_sink_x_1
  id: qtgui_const_sink_x
  parameters:
    affinity: ''
    alias: ''
    alpha1: '1.0'
    alpha10: '1.0'
    alpha2: '1.0'
    alpha3: '1.0'
    alpha4: '1.0'
    alpha5: '1.0'
    alpha6: '1.0'
    alpha7: '1.0'
    alpha8: '1.0'
    alpha9: '1.0'
    autoscale: 'False'
    axislabels: 'True'
    color1: '"blue"'
    color10: '"dark blue"'
    color2: '"red"'
    color3: '"green"'
    color4: '"black"'
    color5: '"cyan"'
    color6: '"magenta"'
    color7: '"yellow"'
    color8: '"dark red"'
    color9: '"dark green"'
    comment: ''
    grid: 'False'
    gui_hint: ''
    label1: ''
    label10: ''
    label2: ''
    label3: ''
    label4: ''
    label5: ''
    label6: ''
    label7: ''
    label8: ''
    label9: ''
    legend: 'True'
    marker1: '0'
    marker10: '0'
    marker2: '0'
    marker3: '0'
    marker4: '0'
    marker5: '0'
    marker6: '0'
    marker7: '0'
    marker8: '0'
    marker9: '0'
    name: '""'
    nconnections: '1'
    size: '1024'
    style1: '0'
    style10: '0'
    style2: '0'
    style3: '0'
    style4: '0'
    style5: '0'
    style6: '0'
    style7: '0'
    style8: '0'
    style9: '0'
    tr_chan: '0'
    tr_level: '0.0'
    tr_mode: qtgui.TRIG_MODE_FREE
    tr_slope: qtgui.TRIG_SLOPE_POS
    tr_tag: '""'
    type: complex
    update_time: '0.10'
    width1: '1'
    width10: '1'
    width2: '1'
    width3: '1'
    width4: '1'
    width5: '1'
    width6: '1'
    width7: '1'
    width8: '1'
    width9: '1'
    xmax: '2'
    xmin: '-2'
    ymax: '2'
    ymin: '-2'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1384, 1472.0]
    rotation: 0
    state: disabled
- name: qtgui_const_sink_x_2
  id: qtgui_const_sink_x
  parameters:
    affinity: ''
    alias: ''
    alpha1: '1.0'
    alpha10: '1.0'
    alpha2: '1.0'
    alpha3: '1.0'
    alpha4: '1.0'
    alpha5: '1.0'
    alpha6: '1.0'
    alpha7: '1.0'
    alpha8: '1.0'
    alpha9: '1.0'
    autoscale: 'False'
    axislabels: 'True'
    color1: '"blue"'
    color10: '"dark blue"'
    color2: '"red"'
    color3: '"green"'
    color4: '"black"'
    color5: '"cyan"'
    color6: '"magenta"'
    color7: '"yellow"'
    color8: '"dark red"'
    color9: '"dark green"'
    comment: ''
    grid: 'False'
    gui_hint: ''
    label1: ''
    label10: ''
    label2: ''
    label3: ''
    label4: ''
    label5: ''
    label6: ''
    label7: ''
    label8: ''
    label9: ''
    legend: 'True'
    marker1: '0'
    marker10: '0'
    marker2: '0'
    marker3: '0'
    marker4: '0'
    marker5: '0'
    marker6: '0'
    marker7: '0'
    marker8: '0'
    marker9: '0'
    name: '""'
    nconnections: '1'
    size: '1024'
    style1: '0'
    style10: '0'
    style2: '0'
    style3: '0'
    style4: '0'
    style5: '0'
    style6: '0'
    style7: '0'
    style8: '0'
    style9: '0'
    tr_chan: '0'
    tr_level: '0.0'
    tr_mode: qtgui.TRIG_MODE_FREE
    tr_slope: qtgui.TRIG_SLOPE_POS
    tr_tag: '""'
    type: complex
    update_time: '0.10'
    width1: '1'
    width10: '1'
    width2: '1'
    width3: '1'
    width4: '1'
    width5: '1'
    width6: '1'
    width7: '1'
    width8: '1'
    width9: '1'
    xmax: '2'
    xmin: '-2'
    ymax: '2'
    ymin: '-2'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2728, 1688.0]
    rotation: 0
    state: enabled
- name: qtgui_const_sink_x_3
  id: qtgui_const_sink_x
  parameters:
    affinity: ''
    alias: ''
    alpha1: '1.0'
    alpha10: '1.0'
    alpha2: '1.0'
    alpha3: '1.0'
    alpha4: '1.0'
    alpha5: '1.0'
    alpha6: '1.0'
    alpha7: '1.0'
    alpha8: '1.0'
    alpha9: '1.0'
    autoscale: 'False'
    axislabels: 'True'
    color1: '"blue"'
    color10: '"dark blue"'
    color2: '"red"'
    color3: '"green"'
    color4: '"black"'
    color5: '"cyan"'
    color6: '"magenta"'
    color7: '"yellow"'
    color8: '"dark red"'
    color9: '"dark green"'
    comment: ''
    grid: 'False'
    gui_hint: ''
    label1: ''
    label10: ''
    label2: ''
    label3: ''
    label4: ''
    label5: ''
    label6: ''
    label7: ''
    label8: ''
    label9: ''
    legend: 'True'
    marker1: '0'
    marker10: '0'
    marker2: '0'
    marker3: '0'
    marker4: '0'
    marker5: '0'
    marker6: '0'
    marker7: '0'
    marker8: '0'
    marker9: '0'
    name: '""'
    nconnections: '1'
    size: '1024'
    style1: '0'
    style10: '0'
    style2: '0'
    style3: '0'
    style4: '0'
    style5: '0'
    style6: '0'
    style7: '0'
    style8: '0'
    style9: '0'
    tr_chan: '0'
    tr_level: '0.0'
    tr_mode: qtgui.TRIG_MODE_FREE
    tr_slope: qtgui.TRIG_SLOPE_POS
    tr_tag: '""'
    type: complex
    update_time: '0.10'
    width1: '1'
    width10: '1'
    width2: '1'
    width3: '1'
    width4: '1'
    width5: '1'
    width6: '1'
    width7: '1'
    width8: '1'
    width9: '1'
    xmax: '2'
    xmin: '-2'
    ymax: '2'
    ymin: '-2'
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2440, 1680.0]
    rotation: 0
    state: disabled
- name: soapy_bladerf_sink_0
  id: soapy_bladerf_sink
  parameters:
    affinity: ''
    alias: ''
    bandwidth: '0.0'
    center_freq: freq
    comment: ''
    dev_args: ''
    freq_correction: '0'
    gain: '20.0'
    samp_rate: samp_rate
    type: fc32
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1648, 1232.0]
    rotation: 0
    state: disabled
- name: soapy_bladerf_sink_1
  id: soapy_bladerf_sink
  parameters:
    affinity: ''
    alias: ''
    bandwidth: '0.0'
    center_freq: freq
    comment: ''
    dev_args: '0'
    freq_correction: '0'
    gain: '20.0'
    samp_rate: samp_rate
    type: fc32
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2736, 1536.0]
    rotation: 0
    state: enabled
- name: soapy_bladerf_source_0
  id: soapy_bladerf_source
  parameters:
    affinity: ''
    alias: ''
    bandwidth: '0.0'
    center_freq: freq
    comment: ''
    dev_args: ''
    freq_correction: '0'
    gain: '20.0'
    maxoutbuf: '0'
    minoutbuf: '0'
    samp_rate: samp_rate
    type: fc32
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1216, 1384.0]
    rotation: 0
    state: disabled
- name: soapy_bladerf_source_1
  id: soapy_bladerf_source
  parameters:
    affinity: ''
    alias: ''
    bandwidth: '0.0'
    center_freq: freq
    comment: ''
    dev_args: ''
    freq_correction: '0'
    gain: '20.0'
    maxoutbuf: '0'
    minoutbuf: '0'
    samp_rate: samp_rate
    type: fc32
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2456, 1544.0]
    rotation: 0
    state: disabled
- name: virtual_sink_0
  id: virtual_sink
  parameters:
    alias: ''
    comment: ''
    stream_id: channel_rx_B
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2464, 1464.0]
    rotation: 180
    state: disabled
- name: virtual_sink_0_0
  id: virtual_sink
  parameters:
    alias: ''
    comment: ''
    stream_id: channel_rx_A
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1592, 1640.0]
    rotation: 180
    state: disabled
- name: virtual_sink_1
  id: virtual_sink
  parameters:
    alias: ''
    comment: ''
    stream_id: channel_tx_A
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1472.0, 1208]
    rotation: 270
    state: disabled
- name: virtual_sink_1_0
  id: virtual_sink
  parameters:
    alias: ''
    comment: ''
    stream_id: channel_tx_B
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2752.0, 1216]
    rotation: 270
    state: enabled
- name: virtual_source_0
  id: virtual_source
  parameters:
    alias: ''
    comment: ''
    stream_id: channel_tx_A
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2000, 1496.0]
    rotation: 180
    state: disabled
- name: virtual_source_0_0
  id: virtual_source
  parameters:
    alias: ''
    comment: ''
    stream_id: channel_tx_B
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2728, 1464.0]
    rotation: 180
    state: enabled
- name: virtual_source_1
  id: virtual_source
  parameters:
    alias: ''
    comment: ''
    stream_id: channel_rx_A
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [1264.0, 1208]
    rotation: 90
    state: disabled
- name: virtual_source_1_0
  id: virtual_source
  parameters:
    alias: ''
    comment: ''
    stream_id: channel_rx_B
  states:
    bus_sink: false
    bus_source: false
    bus_structure: null
    coordinate: [2544.0, 1216]
    rotation: 90
    state: disabled

connections:
- [blocks_repack_bits_bb_1_0_0, '0', pdu_tagged_stream_to_pdu_0, '0']
- [blocks_repack_bits_bb_1_0_0_0, '0', pdu_tagged_stream_to_pdu_0_0, '0']
- [blocks_tagged_stream_mux_0, '0', digital_constellation_modulator_0, '0']
- [blocks_tagged_stream_mux_0_0, '0', digital_constellation_modulator_0_0, '0']
- [blocks_unpack_k_bits_bb_0, '0', digital_correlate_access_code_xx_ts_0, '0']
- [blocks_unpack_k_bits_bb_0_0, '0', digital_correlate_access_code_xx_ts_0_0, '0']
- [channels_channel_model_0_0, '0', virtual_sink_0_0, '0']
- [digital_constellation_decoder_cb_0, '0', digital_diff_decoder_bb_0, '0']
- [digital_constellation_decoder_cb_0_0, '0', digital_diff_decoder_bb_0_0, '0']
- [digital_constellation_modulator_0, '0', qtgui_const_sink_x_0, '0']
- [digital_constellation_modulator_0, '0', soapy_bladerf_sink_0, '0']
- [digital_constellation_modulator_0, '0', virtual_sink_1, '0']
- [digital_constellation_modulator_0_0, '0', virtual_sink_1_0, '0']
- [digital_correlate_access_code_xx_ts_0, '0', blocks_repack_bits_bb_1_0_0, '0']
- [digital_correlate_access_code_xx_ts_0_0, '0', blocks_repack_bits_bb_1_0_0_0, '0']
- [digital_costas_loop_cc_0, '0', digital_constellation_decoder_cb_0, '0']
- [digital_costas_loop_cc_0_0, '0', digital_constellation_decoder_cb_0_0, '0']
- [digital_diff_decoder_bb_0, '0', digital_map_bb_0, '0']
- [digital_diff_decoder_bb_0_0, '0', digital_map_bb_0_0, '0']
- [digital_linear_equalizer_0, '0', digital_costas_loop_cc_0, '0']
- [digital_linear_equalizer_0_0, '0', digital_costas_loop_cc_0_0, '0']
- [digital_map_bb_0, '0', blocks_unpack_k_bits_bb_0, '0']
- [digital_map_bb_0_0, '0', blocks_unpack_k_bits_bb_0_0, '0']
- [digital_pfb_clock_sync_xxx_0, '0', digital_linear_equalizer_0, '0']
- [digital_pfb_clock_sync_xxx_0_0, '0', digital_linear_equalizer_0_0, '0']
- [digital_protocol_formatter_async_0, header, pdu_pdu_to_tagged_stream_0_0, pdus]
- [digital_protocol_formatter_async_0, payload, pdu_pdu_to_tagged_stream_0_0_0, pdus]
- [digital_protocol_formatter_async_0_0, header, pdu_pdu_to_tagged_stream_0_0_1, pdus]
- [digital_protocol_formatter_async_0_0, payload, pdu_pdu_to_tagged_stream_0_0_0_0,
  pdus]
- [epy_block_0, out, epy_block_1, in_tx]
- [epy_block_0, out, epy_block_2, in_tx]
- [epy_block_0, out, epy_block_22_0, in]
- [epy_block_0_0, out, epy_block_1_0, in_tx]
- [epy_block_1, out_rx, epy_block_0, in]
- [epy_block_1, out_tx, epy_block_2, in_tx]
- [epy_block_1_0, out_rx, epy_block_0_0, in]
- [epy_block_1_0, out_tx, epy_block_2_0, in_tx]
- [epy_block_2, out_rx, epy_block_1, in_rx]
- [epy_block_2, out_tx, epy_block_0, in]
- [epy_block_2, out_tx, epy_block_4, in_tx]
- [epy_block_2_0, out_rx, epy_block_1_0, in_rx]
- [epy_block_2_0, out_tx, epy_block_4_0, in_tx]
- [epy_block_4, out_rx, epy_block_2, in_rx]
- [epy_block_4, out_tx, epy_block_7, in_tx]
- [epy_block_4_0, out_rx, epy_block_2_0, in_rx]
- [epy_block_4_0, out_tx, epy_block_7_0, in_tx]
- [epy_block_7, out_rx, epy_block_4, in_rx]
- [epy_block_7, out_tx, epy_block_8, in_tx]
- [epy_block_7_0, out_rx, epy_block_4_0, in_rx]
- [epy_block_7_0, out_tx, epy_block_8_0, in_tx]
- [epy_block_8, out_rx, epy_block_3_1, '0']
- [epy_block_8, out_rx, epy_block_7, in_rx]
- [epy_block_8, out_tx, digital_protocol_formatter_async_0, in]
- [epy_block_8_0, out_rx, epy_block_7_0, in_rx]
- [epy_block_8_0, out_tx, digital_protocol_formatter_async_0_0, in]
- [pdu_pdu_to_tagged_stream_0_0, '0', blocks_tagged_stream_mux_0, '0']
- [pdu_pdu_to_tagged_stream_0_0_0, '0', blocks_tagged_stream_mux_0, '1']
- [pdu_pdu_to_tagged_stream_0_0_0_0, '0', blocks_tagged_stream_mux_0_0, '1']
- [pdu_pdu_to_tagged_stream_0_0_1, '0', blocks_tagged_stream_mux_0_0, '0']
- [pdu_tagged_stream_to_pdu_0, pdus, epy_block_8, in_rx]
- [pdu_tagged_stream_to_pdu_0_0, pdus, epy_block_8_0, in_rx]
- [soapy_bladerf_source_0, '0', blocks_tag_debug_0, '0']
- [soapy_bladerf_source_0, '0', digital_pfb_clock_sync_xxx_0, '0']
- [soapy_bladerf_source_0, '0', qtgui_const_sink_x_1, '0']
- [soapy_bladerf_source_1, '0', qtgui_const_sink_x_3, '0']
- [soapy_bladerf_source_1, '0', virtual_sink_0, '0']
- [virtual_source_0, '0', channels_channel_model_0, '0']
- [virtual_source_0_0, '0', qtgui_const_sink_x_2, '0']
- [virtual_source_0_0, '0', soapy_bladerf_sink_1, '0']
- [virtual_source_1, '0', blocks_tag_debug_0, '0']
- [virtual_source_1, '0', digital_pfb_clock_sync_xxx_0, '0']
- [virtual_source_1_0, '0', digital_pfb_clock_sync_xxx_0_0, '0']

metadata:
  file_format: 1
  grc_version: 3.10.12.0
